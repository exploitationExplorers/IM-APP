---
description: IM 项目全局约定（定位、需求边界、业务红线、协作与文档规范）
alwaysApply: true
---

# IM 项目全局规则

多国手机号即时通讯 App（Android / iOS），单人全栈开发。需求基线：
[新版即时通讯APP-最终需求范围与单人排期分析.md](../../新版即时通讯APP-最终需求范围与单人排期分析.md)。
与该文档冲突时以该文档为准。

## 仓库结构

| 目录 | 内容 | 规则文件 |
|---|---|---|
| `IM-APP-fronend/` | uni-app + Vue 3 客户端 | `im-uniapp.mdc` |
| `IM-APP-server/` | Go + Gin 业务服务 | `im-go-server.mdc` |
| `原型/` | UI 原型图，只读参考 | — |
| 管理后台 | React + shadcn/ui，**尚未创建** | 待补 |

## 技术栈（正式环境，不得擅自替换）

- 业务 API：Go + Gin；业务库：PostgreSQL
- IM 引擎：OpenIM 私有化；消息库：MongoDB（随 OpenIM 部署）
- 缓存 / 在线状态：Redis；消息队列：Kafka；服务协调：Etcd
- 对象存储：MinIO / S3
- 客户端：uni-app（Vue 3）；管理后台：React + shadcn/ui

阶段划分与组件启用时机见
[architecture.md](../../IM-APP-server/docs/architecture.md)，不要提前引入下一阶段的组件。

## 需求边界

必须实现的 8 项：加好友、加群、建群、群内撤回、群内禁止互加好友、转发可选 9999+ 好友、改头像昵称、多国手机号注册。

明确**不做**（不要主动实现、不要预留半成品 UI 或表结构）：转账、钱包、充值提现、红包、朋友圈、VIP 付费、代理分销、商城订单、音视频通话、PC 客户端、H5 / 小程序正式版。

## 业务红线（前后端同时约束）

1. **手机号隐私**：不可按手机号搜索或添加好友；普通用户不可见他人完整手机号；接口返回必须脱敏。
2. **账号唯一性**：账号标识 = 国家区号 + 本地号码，不同国家的相同本地号不得冲突。
3. **群内禁止互加好友**：开关只约束「来自该群的好友申请」，公开 ID 与个人二维码路径不受影响；服务端必须校验，前端隐藏入口不算实现。
4. **撤回**：普通成员仅限自己且在时限内；群主 / 管理员可撤回群内单条；必须记录操作者与操作时间；权限判定在服务端。
5. **转发 / 群发**：单次至少 10000 名好友；接收人之间互不可见；每个有效接收人只收到一份；重复提交不得产生重复任务。
6. **权限校验一律在服务端**，客户端限制只作为体验优化。

## 协作原则

1. 最小改动：只改与当前需求直接相关的代码，不顺手重构无关文件。
2. 简单优先：不为单次使用做抽象；不引入需求未要求的依赖。
3. 遵循现有分层：Go `handler → service → repository`；前端 `pages → stores → api`。
4. 不确定就问，不静默假设需求（尤其是首发国家清单、群发上限等未定项）。
5. 不为绕过报错而降低类型强度或注释掉校验。

## 文档同步（改代码即改文档）

- 接口新增 / 变更 → [api-contract.md](../../IM-APP-server/docs/api-contract.md)
- 架构、阶段、组件变更 → [architecture.md](../../IM-APP-server/docs/architecture.md)
- 新增环境变量 → `IM-APP-server/.env.example` 或 `IM-APP-fronend/src/env.d.ts`

## Git

- 仅在用户明确要求时执行 `git commit`；不主动 push。
- 不提交 `.env`、密钥、`node_modules/`、`dist/`、`bin/`、临时文件。
- 提交信息用中文，一句话说明「为什么」。

## 语言

- 用户可见文案、接口 message、提交信息：简体中文。
- 代码注释：与所在文件既有风格一致；只写约束和意图，不复述代码。
