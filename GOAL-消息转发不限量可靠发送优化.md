# GOAL：消息转发不限量可靠发送优化

> 目标：解决一次转发 40 张图片时只发送部分消息的问题，将现有“达到数量后拒绝”改为“不限制总量、只控制发送速度、可靠排队直到完成”。  
> 范围：`IM-APP-server`、`IM-APP-admin`、`IM-APP-system`、`IM-APP-fronend` 以及对应数据库、OpenAPI、Postman 和测试。  
> 核心约束：本目标只优化功能；已经存在的接口路径和 HTTP 方法保持不变，不进行接口路径规范化改造。

---

## 一、已经确认的产品决策

1. 转发不设置每日、每小时、单次目标数等业务硬上限。
2. 大批量转发可以变慢，但服务端受理后必须可靠排队，不得因为数量过多丢弃后续消息。
3. QPS、并发数和数据库领取批次只用于控制处理速度，不得用于拒绝任务。
4. Kafka 或 OpenIM 暂时不可用时，已经写入 PostgreSQL 的任务继续保留，服务恢复后自动处理。
5. 429、408、网络错误和 5xx 等临时错误持续重试，不因达到固定尝试次数转成最终失败。
6. 账号不存在、群已解散、已被拉黑、无群成员资格、源文件永久丢失等不可恢复问题可以标记为永久失败，并向用户展示明确原因。
7. 单条消息发送失败不得中断同批次剩余消息。
8. 好友和群聊最终统一使用服务端可靠队列；APP 退出后任务仍然继续。
9. 后台不再提供普通的用户转发数量限制；账号违规控制统一使用现有“禁止发送消息”等账号权限能力。
10. 运营暂停只能暂停队列消费，不能拒绝新任务或删除待发送数据。

---

## 二、接口兼容约束

### 2.1 现有接口保持不变

以下已上线接口的路径和 HTTP 方法保持原样，包括现有动态路径和 `PUT` 接口：

- APP Server 的 `/api/v1/forward-tasks`、`/api/v1/forward-task-progress`、`/api/v1/forward-task-targets*`、`/api/v1/forward-tasks/*`。
- Admin Server 的 `/api/admin/v1/forward-tasks/:id*`。
- Admin Server 的 `/api/admin/v1/forward-limits/users/:userId`。
- Admin Server 的 `GET/PUT /api/admin/v1/forward-settings`。
- APP Server 内部的 `/internal/admin/forward-tasks/:id/cancel`、`/internal/admin/forward-tasks/:id/retry`。

不得借本次优化进行以下调整：

- 不重命名现有路径。
- 不修改现有 HTTP 方法。
- 不把现有动态路径迁移成 query 或 JSON body。
- 不删除旧 APP 正在使用的转发接口。
- 不进行与转发可靠性无关的接口风格重构。

### 2.2 新能力

确需增加“多消息批次、群目标、批次进度”等新接口时，使用新增接口承载，不改变旧接口。旧接口在服务端内部适配到新的可靠投递引擎。

新增接口必须满足仓库当前接口规范：

- 查询使用 `GET` + query。
- 写操作使用静态 `POST` + JSON body。
- 不新增动态路径参数。

---

## 三、当前问题

### 3.1 好友转发被错误限流

- APP 前端为每条源消息创建一个独立转发任务。
- 40 张图片转给 1 个好友，会形成 40 个目标投递计数。
- APP Server 默认限制每小时 20、每天 100。
- 当前任务已经包含在统计结果中，提交时又追加一次 `task.TargetCount`，存在重复计数。
- 通常第 20 张提交时失败，后续循环直接停止。
- 提交失败的 draft 任务仍计入额度，重复操作会继续污染额度。
- `ErrForwardLimit` 没有明确映射，客户端只看到通用服务错误。

### 3.2 群转发失败后中断并吞错

- 群转发直接由 APP 调用 OpenIM SDK，不经过后端可靠队列。
- 消息按顺序发送，中间一条失败会中断该群剩余消息。
- 外层捕获后忽略错误，页面已经按成功返回。
- APP 退出、进程被杀或网络切换后，没有可靠恢复机制。

### 3.3 后台配置不是统一数据源

- Admin Server 将全局规则写入 `app_configs`。
- APP Server 对无用户配置的情况仍使用硬编码 `100/20/10000`。
- 管理后台修改全局默认值不一定影响实际发送。
- 用户限额页面与“所有已受理消息最终发送”的产品目标冲突。

### 3.4 临时错误可能被转成最终失败

- Worker 默认最多尝试 8 次。
- 临时故障持续时间超过重试窗口时，消息会被标记失败。
- 提交、恢复和重试操作依赖 Kafka 当前可用，Kafka 不可用时直接拒绝，没有充分利用 PostgreSQL Outbox 的可靠性。

---

## 四、目标架构

```text
APP 提交转发批次
    ↓
PostgreSQL 事务保存批次、消息、目标和 Outbox
    ↓
接口返回“已加入队列”
    ↓
Outbox Publisher 在 Kafka 恢复后发布调度事件
    ↓
Worker 按 QPS、并发和公平策略领取投递记录
    ↓
OpenIM 好友或群消息
    ├─ 成功：记录 clientMsgID/serverMsgID
    ├─ 临时错误：退避后继续重试
    └─ 永久错误：记录明确原因，不影响其他投递
```

可靠性定义：

- HTTP 成功表示 PostgreSQL 已经持久化任务，不表示消息已经全部到达。
- Kafka 只负责调度，PostgreSQL 是任务和进度的事实来源。
- Worker 重启后通过锁超时重新领取 `processing` 记录。
- 每一条“消息 × 目标”拥有确定性 `clientMsgID`，重复消费不会重复发送。

---

## 五、数据结构优化

### 5.1 新增批次模型

建议新增以下表：

#### `forward_batches`

- 一次用户点击“传送”对应一个批次。
- 保存用户、状态、消息数、目标数、总投递数和汇总进度。
- 保存批次级 `idempotency_key`。

#### `forward_batch_messages`

- 保存批次内每条源消息的冻结快照。
- 保存消息顺序，保证40张图片按原顺序投递。
- 不保存仅在本机有效的临时文件路径。

#### `forward_batch_targets`

- 保存好友和群目标。
- `peer_type` 支持 `c2c`、`group`。
- 同一批次内目标唯一。

#### `forward_deliveries`

- 每一条“消息 × 目标”对应一条投递记录。
- 状态支持 `pending`、`processing`、`retrying`、`success`、`permanent_failed`、`paused`、`cancelled`。
- 保存尝试次数、下次重试时间、锁、失败码和 OpenIM 消息 ID。
- 唯一约束为 `(batch_id, message_id, target_id)`。

### 5.2 现有表处理

- `forward_tasks` 和 `forward_task_targets` 继续保留，兼容旧 APP 和现有后台任务查询。
- 旧接口创建的单消息任务由服务层适配为单消息批次，或者由兼容 Worker 继续可靠处理。
- `forward_user_limits` 暂时保留历史数据和审计记录，但 APP Server 停止使用该表拦截发送。
- 不在首个版本删除旧表或旧字段。
- 数据迁移必须使用新增表、`ADD COLUMN IF NOT EXISTS` 和索引增量方式，禁止破坏已有任务。

### 5.3 幂等 ID

投递 `clientMsgID` 使用以下数据确定性生成：

```text
SHA256(batchId + messageId + peerType + peerId)
```

相同批次重复提交、Kafka 重复消费或 Worker 崩溃恢复时，不生成第二条业务消息。

---

## 六、调度配置优化

### 6.1 删除实际拦截

APP Server 不再执行以下业务拦截：

- `daily_limit`
- `hourly_limit`
- `single_targets`
- `enforceLimit()`
- draft/失败任务占用额度
- 临时错误达到固定次数后失败

### 6.2 新的调度配置

| 字段 | 默认值 | 说明 |
|---|---:|---|
| `globalQps` | 20 | 全局每秒发送速度，只影响耗时 |
| `workerConcurrency` | 4 | Worker 并发数 |
| `claimBatchSize` | 50 | 单次领取投递记录数量 |
| `perUserConcurrency` | 2 | 用户间公平调度，防止单个大任务独占 Worker |
| `retryBaseSeconds` | 2 | 首次退避时间 |
| `retryMaxSeconds` | 300 | 退避上限，达到后持续按此间隔重试 |
| `processingLockSeconds` | 300 | Worker 崩溃后的锁恢复时间 |
| `queuePaused` | `false` | 暂停消费但继续接收任务 |
| `retentionDays` | 30 | 已完成投递明细保留时间 |
| `queueAlertDepth` | 100000 | 队列积压告警阈值，不拒绝任务 |

### 6.3 配置事实来源

- 建议由 APP Server migration 创建 `forward_delivery_settings`，作为唯一事实来源。
- 环境变量只作为数据库无配置时的启动默认值。
- Admin Server 通过 APP Server 内部接口读取和修改配置，不再直接维护另一套默认值。
- 配置修改记录管理员、原因、修改前后内容和生效时间。
- Worker 定时刷新配置或通过版本号检测变更，不要求重启服务。

---

## 七、重试和失败分类

### 7.1 无限重试的临时错误

- `context.DeadlineExceeded`
- 网络连接错误
- HTTP 408
- HTTP 429
- HTTP 5xx
- OpenIM 临时服务错误
- Kafka 暂时不可用
- 数据库连接瞬时失败

退避建议：

```text
2s → 4s → 8s → 16s → 30s → 60s → 120s → 300s
```

达到 300 秒后继续每 300 秒重试。`attempts` 仅用于观测和告警，不作为放弃条件。

### 7.2 永久失败

- `user_not_found`
- `group_not_found`
- `group_dissolved`
- `not_friend`
- `not_group_member`
- `blocked`
- `sender_inactive`
- `source_file_missing`
- `invalid_message_content`

永久失败必须返回稳定失败码和可读说明，并允许在外部条件恢复后由用户或管理员手动重试。

### 7.3 源文件可靠性

- 批次只保存可以长期访问的对象存储地址或文件 ID。
- 转发任务未完成前不得清理相关 MinIO 对象。
- Worker 发送前发现源文件不存在时记录 `source_file_missing`。

---

## 八、分项目修改清单

# A. IM-APP-server

## A.1 P0：取消数量拒绝

- [ ] 删除 `ForwardService.Submit` 中的用户日/时/单次限额校验。
- [ ] `Resume`、`Retry` 和 `Submit` 不再因为 Kafka 当前不可用直接拒绝；任务写库和 Outbox 成功即可返回。
- [ ] 保留 QPS、并发、批次大小等速度控制。
- [ ] 修复旧任务中单条失败阻断后续处理的问题。
- [ ] 临时错误不再受 `MaxAttempts` 最终失败限制。
- [ ] 清理 draft 任务占用额度的逻辑，但不删除已有 draft 数据。

## A.2 P0：可靠 Worker

- [ ] 将临时错误和永久错误分类集中到统一函数。
- [ ] 临时错误使用带上限的指数退避并持续重试。
- [ ] 保持 `FOR UPDATE SKIP LOCKED` 和锁超时恢复。
- [ ] 增加用户级公平调度，避免大任务长期占用全部 Worker。
- [ ] 任务无可立即领取记录但仍有未来重试项时，确保后续仍会被唤醒。
- [ ] Worker 处理异常不得导致 Kafka partition 永久阻塞。

## A.3 P1：批次和群目标

- [ ] 新增批次、消息、目标和投递表。
- [ ] 新增批次 Service、Repository、Handler 和 Worker。
- [ ] OpenIM Client 支持 `c2c` 和 `group` 两种 `sessionType`。
- [ ] 保持批次内消息顺序。
- [ ] 旧 `/forward-tasks` 接口内部适配到可靠投递机制，路径和方法不变。
- [ ] 增加批次进度、投递明细和失败重试能力。

## A.4 P1：动态配置

- [ ] 新增服务端调度配置表和读取缓存。
- [ ] Worker 无重启刷新 QPS、并发和重试参数。
- [ ] 新增队列深度、最老待处理时间、重试数量和发送速率指标。
- [ ] 所有后台写操作记录审计原因。

# B. IM-APP-admin

## B.1 保留接口路径

- [ ] 保留现有 `/forward-tasks/:id*` 路径和方法。
- [ ] 保留 `GET/PUT /forward-settings` 路径和方法。
- [ ] 保留 `GET/PUT /forward-limits/users/:userId` 路径和方法。
- [ ] 保留调用 APP Server 的现有内部任务路径。

## B.2 功能调整

- [ ] `/forward-settings` 从数量上限配置改为调度配置。
- [ ] Admin Server 通过 APP Server 内部接口修改实际配置，不直接形成第二套默认值。
- [ ] `/forward-limits/users/:userId` 保留历史查询和审计能力，但配置不再影响 APP Server 投递。
- [ ] 任务详情支持批次消息数、目标数、总投递数、排队数、重试数、成功数和永久失败数。
- [ ] “终止”与普通运维“暂停”分开；暂停不能删除待发送记录。
- [ ] 手动重试支持永久失败项，并记录管理员和原因。
- [ ] 更新 RBAC 权限名称和说明，保留已有权限键的兼容映射。

# C. IM-APP-system

## C.1 转发管理页面

- [ ] “全局转发规则”改为“转发调度与可靠性”。
- [ ] 删除每日、每小时、单次目标上限输入框。
- [ ] 增加 QPS、并发、领取批次、重试间隔、队列暂停和保留天数配置。
- [ ] 明确提示“配置只影响速度，不限制总量”。
- [ ] 增加队列积压、最老等待时间、当前发送速率和重试数量展示。
- [ ] 补齐 `draft`、`retrying`、`partially_completed`、`completed`、`paused` 等真实状态。
- [ ] 任务详情按消息、目标和投递失败三个维度展示。

建议组件边界：

- `ForwardQueueOverview`：队列指标。
- `ForwardDeliverySettingsCard`：调度参数表单。
- `ForwardTaskTable`：任务筛选和列表。
- `ForwardTaskDrawer`：任务进度、目标和失败详情。
- `useForwardOperations`：查询、刷新、暂停、恢复和重试副作用。

## C.2 用户管理页面

- [ ] 删除“转发权限限制”卡片和数量设置入口。
- [ ] 保留现有“禁止发送消息”作为违规账号控制入口。
- [ ] 不再把 `forwardLimit.enabled` 展示成正常业务开关。
- [ ] 历史限额记录如需查看，只在审计记录中展示。

# D. IM-APP-fronend

## D.1 P0：避免部分发送

- [ ] 好友转发循环中某个任务提交失败时，记录已提交和未提交消息，不错误显示全部成功。
- [ ] 群聊逐条发送时，单条失败不得中断后续消息。
- [ ] 删除吞掉群转发异常的空 `catch`。
- [ ] 不再在 `void sendToGroups()` 尚未完成时标记全部成功。
- [ ] 展示“已加入队列”而不是将服务器受理等同于全部送达。

## D.2 P1：统一批次

- [ ] 一次选择的40张图片生成一个批次和有序消息列表。
- [ ] 好友、群聊都提交给服务端可靠队列。
- [ ] APP 退出后不影响后端继续发送。
- [ ] 支持查询批次进度并恢复到本地状态。
- [ ] 展示排队、发送中、重试中、成功和永久失败数量。
- [ ] 支持对永久失败项发起手动重试。
- [ ] 只在所有可投递项完成后显示“转发完成”。

---

## 九、后台页面和配置迁移

1. 首次上线先读取旧 `forward.settings`，但不再将数量字段用于拒绝发送。
2. 写入新的调度字段后保存配置版本。
3. 管理端新版本不再提交数量限制字段。
4. 旧管理端短时间内仍可访问原路径，但其数量修改不影响实际投递。
5. 管理后台必须明确展示“当前为不限总量模式”。
6. `forward_user_limits` 历史数据保留，不自动清空。
7. 稳定运行并确认不需要回滚后，再单独评估历史字段归档，不纳入本目标首版。

---

## 十、上线顺序

### 阶段一：立即止损

1. APP Server 停止数量限额拦截。
2. APP Server 接受 Kafka 暂不可用时的任务并持久化 Outbox。
3. Worker 对临时错误持续重试。
4. APP 修复群转发中断、吞错和提前成功提示。
5. 增加40张图片回归测试。

### 阶段二：统一可靠队列

1. 执行新增批次表 migration。
2. 部署支持好友和群目标的 APP Server。
3. 部署 Admin Server 调度配置和队列查询。
4. 部署管理端新的转发调度页面。
5. 发布 APP 批次转发版本。
6. 对无法通过后端修复的旧 APP 群转发问题评估最低版本或强制升级。

### 阶段三：观测和收尾

1. 观察队列深度、最长等待时间、429、5xx 和永久失败率。
2. 根据 OpenIM 实际承载能力调整 QPS，不改变不限总量原则。
3. 增加完成数据归档任务。
4. 稳定后标记旧数量限额为历史功能。

---

## 十一、测试要求

### 11.1 APP Server

- [ ] 40张图片转发给1个好友，最终40条成功。
- [ ] 40张图片转发给多个好友，投递总数等于 `消息数 × 目标数`。
- [ ] 设置 QPS=1 后全部发送，只增加完成时间。
- [ ] Kafka 停机期间提交成功进入数据库，恢复后自动发送。
- [ ] OpenIM 连续返回429、500或超时，恢复后自动补发。
- [ ] Worker 在第20条时重启，恢复后继续且不重复。
- [ ] 同一幂等键重复提交不重复发送。
- [ ] 单条永久失败不影响同批次后续消息。
- [ ] `processing` 锁过期后可被其他 Worker 恢复。
- [ ] 暂停队列时可以继续提交，恢复后继续发送。

### 11.2 群聊

- [ ] 40张图片转发到1个群，最终40条成功。
- [ ] 中间一条发生临时错误，后续消息仍被处理。
- [ ] APP 提交后退出，服务端继续完成群消息投递。
- [ ] 群解散或发送者不在群内时返回稳定永久失败码。

### 11.3 管理后台

- [ ] 修改 QPS 后 Worker 无重启生效。
- [ ] 页面不再出现每日、每小时和单次目标上限。
- [ ] 用户详情不再提供转发数量限制。
- [ ] 队列暂停期间任务数量增长但不丢失。
- [ ] 恢复后积压任务持续下降。
- [ ] 所有配置和任务操作都有审计记录。
- [ ] 原有接口路径和 HTTP 方法契约测试全部通过。

### 11.4 前端

- [ ] 不提前显示“全部成功”。
- [ ] 不存在空 `catch` 吞掉群转发错误。
- [ ] 可以查看批次实时进度。
- [ ] 永久失败项显示明确原因并支持重试。
- [ ] Vue TypeScript 类型检查和生产构建通过。

---

## 十二、文档同步

- [ ] 更新 `IM-APP-server/docs/openapi.yaml`。
- [ ] 更新 API contract。
- [ ] 更新 Postman/Apifox 集合。
- [ ] 更新 Kafka、Worker 和 Outbox 架构说明。
- [ ] 标记现有数量限额字段为“历史字段，不参与发送拦截”。
- [ ] 明确区分“任务已受理”“投递处理中”“投递完成”。
- [ ] 文档中保留原有接口路径，不做接口规范迁移。

---

## 十三、完成标准

本目标只有在以下条件全部满足后才算完成：

1. 默认每小时20、每天100、单次目标数等配置不再阻止消息转发。
2. 40张图片无论发给好友还是群聊，都不会因中途临时错误丢掉后续消息。
3. 已受理任务在服务重启、Kafka故障、OpenIM临时故障后能够自动恢复。
4. 临时错误持续重试，永久错误有明确原因和人工重试入口。
5. APP、APP Server、Admin Server、管理端展示的任务状态和进度一致。
6. 后台配置只控制速度、并发、退避和暂停，不控制总量。
7. 现有接口路径和 HTTP 方法没有因本目标发生变化。
8. OpenAPI、Postman、数据库 migration 和自动化测试与实际实现一致。

---

## 十四、明确不做

- 不修改现有接口路径和 HTTP 方法。
- 不借机重构其他业务模块接口。
- 不删除历史转发任务和限额审计数据。
- 不承诺向不存在的账号、已解散群或被业务规则禁止的目标强制投递。
- 不把提高 QPS 当成解决丢消息的唯一手段。
- 不允许通过静默吞错或提前显示成功掩盖未完成投递。
