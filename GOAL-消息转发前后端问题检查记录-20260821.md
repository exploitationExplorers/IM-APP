# 消息转发前后端问题检查记录

> 检查日期：2026-08-21
> 检查范围：`IM-APP-fronend`（转发页 + store + api + 快照）与 `IM-APP-server`（handler / service / repository / worker / migration / OpenIM client / Kafka）转发全链路
> 基线：`GOAL-消息转发不限量可靠发送优化.md`
> 结论：后端投递引擎（不限量、幂等、无限重试、锁恢复、单条失败不中断）已扎实落地；主要遗留在**前端仍按“同步立即成功”模型使用异步可靠队列**，以及 Kafka 链路未接线。

---

## 一、完整逻辑链路（现状）

**前端提交**
1. `room.vue` 多选消息 → `useChatMessageActions.goForward()` → `forwardStore.start(conversationId, messageIds)` → 跳 `forward.vue`（useChatMessageActions.ts:155）
2. `forward.vue` 选目标 → `collectPlan()` 生成 friendPlan(ids/generate/all_friends) + groupTargets
3. `onSend()`：过滤已解散群 → `buildSources()` 冻结快照 → `resolveGroupTargetIds()` → `forwardStore.submitBatch()`
4. `submitBatch` → 一次 `POST /forward-batches` → toast「已加入队列」→ 返回来源页

**后端受理** `service.CreateBatch`（service/forward.go:65）
- 循环每条消息：`Create`(insert forward_tasks draft，幂等键 `batchKey-i`) → GenerateTargets/RemoveTargets/AddGroupTargets → `Submit`
- `SubmitTask`（repository/forward.go:332）：draft→pending + 写 forward_task_actions + 写 forward_kafka_outbox（同一事务）

**投递执行** `ForwardWorker.RunPolling`（service/forward.go:433，DB 轮询，不依赖 Kafka）
- 每 2s：GetDeliverySettings → 未暂停则 ClaimRunnableTargets（跨用户公平 + FOR UPDATE SKIP LOCKED）→ processBatch（QPS 限速 + N goroutine）→ processTarget
- processTarget：查任务态 → eligibility 校验 → 确定性 clientMsgID → SendForwardMessage(sessionType=1)/SendForwardGroupMessage(sessionType=3) → 成功记录 / 临时错误退避重试 / 永久错误标记失败
- finalizeTaskTx：done_count>=target_count 时结算 completed / partially_completed / failed

---

## 二、已正确落实（无需改动）

- ✅ 不限量：service 层无 daily/hourly/single 限额拦截
- ✅ 确定性幂等 clientMsgID = sha256(taskID + peerType:targetUserID)（forward.go:753）
- ✅ 临时错误无限重试（forwardRetryDelay attempt≥31 封顶不放弃，forward.go:736）
- ✅ MaxAttempts 仅观测不终止（retryOrFail 不检查 attempts）
- ✅ 锁超时恢复 locked_until<NOW() 重领
- ✅ 单条失败不中断批次（每 target 独立事务结算）
- ✅ 群/单聊 sessionType 正确（3/1），notOfflinePush=true
- ✅ Kafka 不可用不阻断：Submit 只写 DB，RunPolling 不依赖 Kafka
- ✅ QPS/并发除零、死锁被 migration 027 的 CHECK (>0) + 默认行 INSERT 防住

---

## 三、待修复问题（按影响排序）

### 🔴 P0-1 提示语矛盾 + 违反“禁止提前显示成功”红线
一次转发用户连续看到两个矛盾提示：
- `forward.vue:403` toast「已加入队列」（正确，仅受理）
- 返回来源页 → `room.vue:257` `onShow` 里 `consumeSucceeded()` 触发 → `room.vue:1245` 弹「转发成功」

后端 `/forward-batches` 语义是“已持久化入队”，非“已送达”。前端却显示“转发成功”，违反设计文档：“不允许提前显示成功掩盖未完成投递”“只在所有可投递项完成后显示转发完成”。
**后果**：部分永久失败（拉黑/群解散）时用户看到“成功”，永不知情。
**最小修复**：将 `room.vue` 的「转发成功」文案改为「已加入发送队列」等中性提示。

### 🔴 P1-2 提交后无任何进度/结果追踪（设计 D.2 整段缺失）
`onSend` 成功即 `clear()` + 返回，`lastTaskIds` 无页面消费。API 层 `fetchForwardTaskProgress` / `fetchForwardTaskTargets` 已实现但前端无调用点。用户看不到：
- 排队 / 发送中 / 重试中 / 成功 / 永久失败 数量
- 永久失败原因（后端已返回明确码：not_friend / group_dissolved / blocked / not_group_member 等）
- 无手动重试入口
**修复**：新增转发任务进度页（列表 lastTaskIds → 轮询 progress + targets，展示计数与失败原因 + 失败重试按钮）。

### 🟠 P2-3 部分失败会重复发送
`CreateBatch` 非原子（逐条 Submit，service/forward.go:78-116）。中途失败时前端 catch 显示“提交失败”，但已入队部分仍投递。用户重试时 `submitBatch` 用新的 `createIdempotencyKey()`（forwardSnapshot.ts:57）→ 幂等键全变 → 已入队消息被重复发送。
**修复方向**：一次转发会话内固定 idempotencyKey（失败重试复用），或后端整批落库单事务 / 返回已受理 taskIds 供前端恢复。

### 🟠 P2-4 一个群解析失败导致整批全失败
`resolveGroupTargetIds`（forward.vue:369）任一群解析不出 businessGroupId 即 throw → `onSend` 进 catch → 所有目标（含正常好友）都不发。
**修复方向**：单个群解析失败降级为跳过该群 + 提示，不阻断其余目标。

### 🟡 P3-5 历史消息转发可能报“原消息不存在”
`buildSources` 依赖 `chatStore.getRawMessage(id)`（内存缓存 rawMessages，chat.ts:950）。多选已被清理出缓存的旧消息 → undefined → 抛错。
**修复方向**：快照缺失时按需回源拉取，或选择阶段就冻结快照。

### 🟡 P3-6 OpenIM 发送阶段永久失败码不细分
`processTarget` OpenIM 发送失败统一 code="openim_send_failed"（forward.go:708）。发送瞬间才变化的永久错误（刚被踢群等）落到笼统码，前端无法精确展示。发送前 eligibility 校验已覆盖大部分场景。

### ⚪ P4-7 死代码
- 前端：`forwardStore.submitFriendPlan` / `createAndFillTask`，api 的 `createForwardTask` / `addForwardTaskTargets` / `generateForwardTaskTargets`（已被 submitBatch 取代，不再调用）
- 后端：Kafka 消费链路 `ForwardWorker.Run` / `consumeDelivery` / `RunTaskOnce` 无调用点（见下节）

---

## 四、Kafka 架构澄清

存在**两套互不相干的 Kafka**：
- **OpenIM 自带 Kafka**（IMENV_KAFKA_ADDRESS，deploy/openim/docker-compose.yml:169）：OpenIM 内部使用，server 从不直接操作；server 发消息一律走 HTTP `/msg/send_msg`（client.go:637）。
- **server 转发 Kafka**（KAFKA_BROKERS + topic im-forward-tasks，config.go:104）：server 为自建转发队列准备。

**当前状态**：
- 投递靠 `RunPolling` 直接轮询 PostgreSQL，**不需要 Kafka**。
- 消费端 `ForwardWorker.Run` 在 main.go **从未启动**（只启动 RunPolling，main.go:390）。
- 默认 `.env` 中 `KAFKA_BROKERS=` 为空，Kafka 关闭，功能正常。
- 若开启 Kafka：OutboxPublisher 持续发布事件到 topic 却**无消费者** → 空转浪费。
- 无论开关，SubmitTask 都会写 forward_kafka_outbox 表（repository:360），关 Kafka 时无人消费会以 pending 堆积（无害但增长）。

**残留待办**：
- `README.md:94` “KAFKA_BROKERS 未配置时转发返回 503” 为旧文档，代码已改为无 Kafka 也受理，需更正。
- 决策：① 精简为 DB 轮询单一路径（移除 Kafka 发布链路 + outbox 写入）；或 ② 保留 Kafka 做低延迟唤醒则需在 main.go 接上 `ForwardWorker.Run`。倾向 ①。

---

## 五、建议修复优先级

1. **P0-1**（改文案，最小成本、立刻消除“提前显示成功”）
2. **P2-3 / P2-4**（重复发送、整批失败，影响生产可靠性与体验）
3. **P1-2**（进度追踪页，兑现设计核心价值，工作量较大）
4. **Kafka 精简 + README 更正**（架构清理）
5. **P3 / P4**（失败码细分、历史消息回源、死代码清理）
