# 复制为 sync-server.local.env 后填写（不要提交含真实 IP/密钥的文件）
# 在 PowerShell 中加载：
#   Get-Content .\scripts\sync-server.local.env | ForEach-Object {
#     if ($_ -match '^\s*#' -or $_ -notmatch '=') { return }
#     $k,$v = $_ -split '=',2
#     Set-Item -Path "Env:$($k.Trim())" -Value $v.Trim()
#   }

VIRON_DEPLOY_HOST=你的服务器IP或域名
VIRON_DEPLOY_USER=root
VIRON_DEPLOY_PATH=/root/IM-APP/IM-ops/viron
# VIRON_DEPLOY_IDENTITY=C:\Users\你\.ssh\id_rsa
