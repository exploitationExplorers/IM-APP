# Viron 环境运维平台实现文档

文档版本：1.0
状态：持续维护
部署方式：Docker Compose，Lite/Full 主服务镜像 + 隔离脚本 Runner，元数据库可选 SQLite WAL 或 MySQL/MariaDB

## 1. 产品目标

Viron 是一个支持个人与组织协作的环境运维工作台。它以“环境”为核心，将分散的 Web 页面信息、SSH 主机、数据库与 Redis 连接及其凭据、业务服务与部署节点，以及 Markdown 运维知识组织到一起，并在浏览器和桌面 App 中提供真实 SSH 终端、SFTP 文件管理、MySQL/MariaDB 数据库维护、Redis 键空间维护、服务维护和环境知识库能力。

Viron 是唯一产品品牌。环境变量、Compose 服务和镜像统一使用 Viron 命名；旧 `ENVMAN_*` 环境变量不再兼容，检测到后启动失败并提示对应的 `VIRON_*` 名称。Cookie、浏览器存储键、SQL 语句边界和 `/data/envman.db` 等持久化格式继续保留原值，避免破坏已有实例数据。

Web、桌面共享界面、浏览器 favicon 和 App 图标统一使用 `design/logo/viron-logo.svg` 的轨道 V 标志；App 图标资源仅在透明方形画布内居中该标志，不改变主体轮廓。

首版解决以下问题：

1. 用环境组和环境统一组织项目资源。
2. 在 Viron 内直接使用 Web 页面，并同时保持多个账号的独立登录状态。
3. 从 SecureCRT、Navicat 或手工录入批量获得连接信息。
4. 将导入的连接放入资源池，并批量关联到一个或多个环境。
5. 在浏览器中同时打开多个真实 SSH 会话并分屏操作。
6. 在浏览器中完成 MySQL/MariaDB 对象浏览、SQL 执行、数据与结构编辑。
7. 用 Docker Compose 交付 Lite/Full 主服务与隔离脚本 Runner，业务数据持久化到 `/data`。
8. 在同一权限与审计边界内浏览、维护和诊断 Redis。
9. 为每个工作空间维护可分层分类、在线编辑、预览和授权的 Markdown 知识库，并通过多环境标签生成环境视图。
10. 以业务服务聚合多台 SSH 主机上的部署节点、当前状态、宿主机指标、日志入口和标准维护动作，并通过 Linux `viron-monitor` 在断网期间持续缓冲采集结果。

## 2. 已确认的产品边界

### 2.1 首版包含

- 平台用户注册、登录，并在个人信息中查看用户名、平台角色、账号 ID 和注册时间、修改密码及退出登录；平台管理员创建/停用账号和重置密码。
- 个人与组织工作空间、组织管理员/成员、项目成员以及环境组、环境和连接的直接/项目授权。
- 可选的一层环境组；环境可以不属于任何组。
- 环境的创建、编辑、删除、搜索、筛选和状态展示。
- 每个环境关联多个 Web 页面、SSH 连接和数据库连接。
- Web 页面保存多个账号、密码、备注和自定义字段；每个账号对应独立持久化 Chrome Profile。
- Web 账号列表切换、单账号视角、服务端页面画面、自动填充、文件上传下载和子页面切换。
- 工作空间级 SSH 密钥库；个人所有者和组织管理员可导入、生成、导出和维护多把密钥，连接按 ID 引用密钥，旧版内嵌私钥保持兼容。
- SSH 密码、托管密钥、旧版内嵌私钥、keyboard-interactive、单级跳板机。
- 隔离脚本全资源同步、SecureCRT 同步源、文件/目录 ZIP 导入、手工 SSH 连接。
- Navicat 15–17 NCX/XML/ZIP 导入和手工数据库连接。
- 统一连接资源池、待分配队列、批量环境关联、文件夹映射规则。
- SSH、数据库与 Redis 连接的手工批量巡检，并保存最近一次结果和耗时。
- 连接资源池通过“数据处理”入口进入连接工具页，集中提供同步源管理、个人连接/环境/环境组向组织的原子复制与授权，以及连接巡检。
- 多 SSH 会话、断线恢复、单窗/左右/上下/四宫格、拖拽分屏、布局持久化。
- SSH 登录脚本、按用户和连接隔离的本地命令历史、ZMODEM `rz`/`sz` 文件传输。
- SFTP 浏览、上传、下载、重命名、删除、创建目录和权限修改。
- 环境内基于 SSH 的受控多文件实时日志查看、行数上限、grep 风格关键字过滤、客户端语义和关键字高亮，以及当前屏幕日志下载。
- 环境服务维护：业务服务与多部署节点建模，systemd、Docker、Podman、Supervisor 自主扫描与标准启停，基于目标机 kubeconfig/context 的 Kubernetes 工作负载扫描，普通进程手工登记，日志快捷关联，服务级与节点级自定义脚本功能按钮，宿主机资源趋势、进程组成与自动性能诊断，按服务平铺的部署节点 CPU、内存、运行时间和重启次数时间序列，以及环境级宿主机离线、CPU、内存、磁盘使用率、温度、磁盘挂载变化和部署状态告警。
- Linux `viron-monitor` 的 `amd64/arm64` 静态产物、SQLite/WAL 30 天离线缓冲和单调序列读取协议；中心服务只经已有 SSH/跳板链路拉取，并按“工作空间 + Agent”共享游标和中心样本，不要求目标机器开放新端口或访问 Viron。
- 工作空间级 Markdown 知识库、任意层级文件夹、多环境标签及文件夹标签继承、环境内关联文档、拖动移动、在线编辑/分栏/预览、显式保存与 1 秒自动保存、并发冲突保护、Base64 图片、Markdown/ZIP 导入导出，以及按文件夹或文档分配给成员/项目组的编辑权限。
- MySQL、MariaDB 的 TCP、SSH Tunnel、SSL/TLS 和 HTTP Tunnel 连接。
- Redis Standalone 的 TCP、TLS、复用现有 SSH 连接的 SSH Tunnel、ACL 用户名/密码、逻辑库和只读连接策略。
- Redis 安全 `SCAN` 键浏览、六种核心类型的查看与常用维护、二进制 UTF-8/JSON/Hex/Base64 解释、键复制/重命名/删除/TTL、Monaco 命令工作台、`INFO` 和受限 `SLOWLOG GET`。
- 数据库、表、视图、存储过程、函数、触发器和事件的查看与编辑。
- SQL 多页签、高亮、补全、格式化、选中/全部执行、EXPLAIN、历史、收藏、取消。
- 用户级数据库与数据表收藏，并从连接树顶部快速直达目标。
- 可编辑数据网格、分页、排序、筛选、固定列、批量增删、复制、导入导出。
- CSV、XLSX 数据导入，CSV、XLSX、SQL 导出及 SQL 备份恢复。
- 数据库/表 SQL 备份与恢复、任务进度和日志。
- MySQL/MariaDB 连接间数据传输、数据同步和结构同步。数据同步要求源表和目标表具有相同主键及兼容列，按主键分批读取并分别执行插入、更新和删除，每张目标表使用独立事务；结构同步先生成对象级差异和 SQL 预览，再同步表列、主键、索引、外键、检查约束、字符集/表选项、视图、函数/存储过程、触发器和事件。删除、替换及删除目标多余对象必须在预览中显式选择，无法安全生成 SQL 的分区或表达式索引差异只报告、不自动执行。
- 登录、凭据、连接、SSH、SFTP、SQL、导入和配置审计。
- 终端录像与 SQL 历史默认保留 30 天，可配置。
- 浅色/深色/明亮主题；明亮主题除最外侧用户菜单栏外统一使用浅色业务界面，桌面优先，平板可用，手机可查看基础信息。
- 平台数据手工导出与恢复，不做每日自动备份。
- Viron MCP 远程 Streamable HTTP 与桌面 STDIO 双入口；Codex 等 Agent 在现有用户、工作空间、权限、凭据、确认、审计和执行 Runtime 边界内调用除账号安全、权限控制和秘密读取/导出之外的 Viron 用户业务能力。
- `linux/amd64`、`linux/arm64` 镜像和离线镜像包。

### 2.2 首版不包含

以下候选方向的当前规划见 [docs/ROADMAP.md](./docs/ROADMAP.md)。

- 审批流、SSO、LDAP、OIDC、MFA 和 WebAuthn。
- 通用 SSH 本地/远程/动态端口转发。
- SSH Agent 转发。
- PostgreSQL、Oracle、SQL Server 或其他数据库。
- 定时数据库数据同步和双向实时数据库同步；SecureCRT 连接目录仍支持 Cron 定时拉取。
- 验证码、短信、MFA、WebAuthn 和跨域 SSO 的自动完成。
- 本地 Agent 或 server/agent 混合浏览器执行。
- 内置 Viron Agent 已使用 Pi 作为持久多轮与 ReAct/tool-loop 内核，按设备、Endpoint 与用户保存脱敏文本历史，并通过 Viron MCP 紧凑网关调用已注册业务 Operation；本机 SSH 单行命令与 Shell 脚本安全填入、本机与服务端转发 SSH/数据库多步只读诊断、SSH 写命令与数据库写 SQL 的影响预览和确认执行，以及双执行位置已交付。日志、Redis、Web、知识库和服务维护的通用业务调用以 MCP 目录实际注册能力为准，领域专用可见工作台适配器、远程脚本文件创建或执行、数据库批处理任务、Redis 写和 Web 提交仍需继续补齐。服务端转发不提供命令或脚本填入。
- 日志时间过滤、日志历史保存和跨文件日志时间排序。
- 手机端终端分屏和数据库编辑。
- 平台数据定时自动备份。
- 多实例高可用部署。
- 环境组服务汇总，以及脚本化部署、发布和回滚动作。

## 3. 用户与使用规模

### 3.1 用户

用户是平台级账号。注册后自动拥有个人工作台，可以加入多个组织，并在个人与组织工作空间之间显式切换。当前工作空间保存在登录会话中，决定资源查询、新建归属、工作台会话和审计范围。

平台管理员负责创建、停用和重置平台账号，但不会自动获得其他用户个人资源或组织资源权限。每个组织至少有一名组织管理员；普通成员只拥有直接授权与所属项目授权的并集。组织管理员可以管理本组织全部资源，普通成员可以使用获授权的 Web、SSH、SFTP 和数据库能力，但不能查看明文凭据或继续授权；除知识库中自己创建或取得编辑授权的节点外，普通成员不能修改组织资源。

知识库是组织资源修改规则的明确扩展：组织普通成员可以查看当前组织工作空间的全部文档、创建文档，并持续编辑、重命名、移动或删除自己创建的文档；组织管理员可以把文件夹或单篇文档的编辑权分配给成员或项目组。文件夹授权向当前和未来的全部后代继承，文档授权只增加该文档编辑权，不提供显式拒绝规则；组织管理员始终拥有全部知识库权限。修改节点的环境标签还要求节点编辑权与对应环境访问权。

组织成员不通过平台用户名搜索或由管理员直接加入。组织管理员为邀请链接组合设置 1 小时、24 小时、7 天或 30 天有效期，以及 1、3、5、10、不限或 1–10000 的自定义可加入人数；链接可以在管理列表中重新复制或手工撤销。服务端保存随机令牌的 SHA-256 摘要，并使用平台主密钥加密保存可恢复令牌，只有目标组织管理员能通过列表接口读取。受邀者登录后可以直接打开链接，也可以在 Web 或 macOS App 的“组织与用户”页面粘贴完整链接，随后进入同一邀请确认页面，确认邀请人与组织并同意加入；有效期结束、名额耗尽或撤销时拒绝加入。新成员固定以普通成员身份加入，并记录具体邀请和邀请人；管理员可以在加入后再调整组织角色和分配资源。

首次启动通过环境变量创建初始平台管理员：

- `ADMIN_USERNAME`
- `ADMIN_PASSWORD`
- `ALLOW_WEAK_PASSWORDS`

初始平台管理员由 `ADMIN_USERNAME` 和 `ADMIN_PASSWORD` 创建。密码可以在页面修改，也可以由平台管理员或 CLI 重置；系统不提供邮件找回密码。`ALLOW_WEAK_PASSWORDS=false` 是生产默认策略，要求密码至少 10 个字符，并包含大写字母、小写字母、数字、特殊字符中的至少 3 类；首次管理员、注册、平台建号、修改、重置和迁移 CLI 使用同一规则。本地开发可显式设为 `true`，此时只拒绝空密码。策略只约束密码写入，不会让已有密码自动失效；密码始终使用 Argon2id 保存。

### 3.2 设计容量

- 最多 1,000 个环境。
- 最多 10,000 个连接。
- 最多 20 个同时在线 SSH 会话。
- 最多 10 个同时执行的数据库查询。
- 单用户默认最多 30 个活动连接，由 `USER_CONNECTION_LIMIT` 调整；Web 账号实例、SSH 终端、日志流、SFTP 传输和数据库逻辑会话分别计数，并跨设备、客户端、执行模式与工作空间累计。Redis 当前按请求建立受限短连接，不作为可长期驻留的逻辑会话计数。
- Web 账号页面默认全局最多 8 个活动 Chromium，可通过 `WEB_VIEW_TOTAL_LIMIT` 调整；环境详情内已访问账号保留 Chrome Context，隐藏账号暂停画面流，页面切换不释放运行实例。
- SSH 日志流默认每个用户最多 3 路、平台最多 10 路；浏览器单个日志画面最多保留最近 5,000 行。
- 所有连接默认在连续 30 分钟无实际收发数据后断开，由 `CONNECTION_IDLE_MINUTES` 统一调整。超过用户或分项并发限制时明确提示，不静默抢占已有会话。

## 4. 信息架构与页面

### 4.1 登录页

- Viron 品牌、用户名、密码、登录状态提示，以及自主注册入口。
- 不展示邮件找回或第三方登录入口。

### 4.2 环境总览

- 顶部提供环境名称、当前用户别称和标签搜索，以及新建环境、新建环境组操作。
- 环境列表最上方固定提供“收藏”分组，完整复用环境卡片；点击卡片地址栏星标切换当前用户收藏，环境仍保留在原分组中。收藏与组织环境别称共用“用户 + 环境”偏好记录，个人和组织工作空间均支持，并随账号跨 Web、macOS 和 Windows 客户端同步。
- 环境组为一层目录导航，并提供固定在末尾的“未分组”入口；环境组可拖动排序，环境卡片可在组内排序或拖到其他组，顺序保存到当前工作空间。搜索筛选期间禁用排序，避免以不完整列表覆盖完整顺序。
- 每个环境卡片显示说明、标签、Web/SSH/数据库/Redis 数量和最近更新时间。组织环境卡片的浏览器式 Tab 优先显示当前用户按“用户 + 环境”保存的个人别称，地址栏、环境详情和共享资源始终使用环境原名；别称置空时删除个人偏好并回退到原名。

### 4.3 环境详情

七个已开放主工作区：

1. Web 入口：页面地址列表、左侧账号列表、单/双账号视角和嵌入式浏览器工作区。
2. SSH 工作台：主机树、会话页签和分屏终端，并内置双主机 SFTP 文件管理与主机间传输模式。
3. 日志：选择当前环境的 SSH 连接和 1–10 个绝对文件路径，设置行数上限后查看受控的实时 `tail` 输出，并可按关键字过滤和下载当前屏幕结果。
4. 数据库工作台：连接树、对象树、SQL 页签、数据表与结果集。
5. Redis 工作台：连接树、逻辑库、游标键扫描、类型化值维护、受控命令和运行诊断。
6. 知识库：任意层级文档树、Markdown 编辑/分栏/预览、自动保存状态、图片、导入导出和节点编辑授权。
7. 服务维护：服务清单、多主机部署节点、SSH 宿主机监控与服务发现页签、扫描候选、日志快捷入口、标准启动/停止/重启动作，以及服务级和节点级自定义脚本功能按钮。

普通环境详情中的七个工作区使用统一外部尺寸。页签、菜单和路由切换只停用或隐藏工作区，不销毁已访问的环境工作区实例；已打开的 Web 页面、SSH 会话、数据库工作状态、Redis 工作台筛选、日志流、知识库草稿和服务维护上下文继续保留，知识库离开时也会立即尝试保存。返回原环境后恢复此前现场。浏览器刷新、浏览器关闭重开或 App 重启不保证恢复活动连接。

环境详情加载完成后使用有界后台预取降低首次页签等待。只有当前前台环境、文档可见且浏览器未启用省流模式时才调度；日志、知识库、Redis、服务维护和 SSH 的异步代码与只读列表按单并发、分段延迟顺序预取，空页签跳过。数据库工作台及其编辑器体积较大，不进入自动后台队列，只在用户持续悬停、键盘聚焦或触摸数据库页签时提前加载。预取 GET 结果按当前登录和工作空间使用一次性、短有效期内存缓存，写操作、登录、退出或工作空间切换立即清空，不持久化，也不自动建立 SSH、数据库、Redis 或日志连接。

Web 是唯一允许建立真实连接的默认预热项：前台 Web 工作区稳定约 1.5 秒后，只预热当前入口默认选中的第一个账号。预热沿用当前执行方式、权限、凭据隔离、活动连接和 Chromium 数量限制，但保持待访问外观；普通 Web 不启动截图画面流，桌面 App 不显示原生 WebContentsView。未发生页面指针或键盘交互时仍归预热调度所有，切换页签、离开环境、进入后台或卸载工作区会立即关闭 Chromium 和活动连接登记，但保留既有 Profile/Cookie。用户一旦操作页面，该实例转为普通已访问账号并开始显示画面，沿用环境工作区保留语义；不会自动预热第二个账号。

环境详情进入沉浸模式后隐藏环境标题和顶部页签，以单个纵向树形浮层导航当前工作区。折叠态只显示蓝底白色箭头把手，把手可自由拖动并在松手时吸附到窗口左侧、右侧或顶部；位置仅在本次沉浸会话中保留，重新进入时回到右侧中部。展开态固定在当前吸边位置，点击浮层外部时收起且原点击继续作用于下方工作区。Web 使用“Web 入口 → 网站 → 登录账号”三级树，同一时间只展开一个网站；账号及 SSH、日志、数据库、Redis、知识库、服务维护叶子切换后自动收起，服务维护分别显示服务数和已监控宿主机数。`Esc` 在展开时先收起浮层，在折叠时退出沉浸模式；App 的原生网页获得焦点时也由主进程执行同一状态转换。浮层底部和 Web 地址栏尾部按钮均与同一沉浸状态联动并提供退出入口。切换沉浸模式只改变导航与布局，不销毁或重建任何工作区现场。独立 SSH、数据库和 Redis 页面继续使用原有沉浸模式。

SSH、数据库和日志配置列表支持在连接项上右键打开操作菜单。右键入口与连接项原有的按钮或行内操作共用同一动作定义，菜单只作用于当前连接项；Element Plus 浮层负责视口内定位、外部点击与 `Esc` 关闭，原按钮继续提供可聚焦的键盘入口。

### 4.4 当前连接

- 侧栏底部以“当前连接数/单用户最大连接数”替代原服务正常状态块，点击进入当前连接页；侧栏、列表、超限判断和真实运行资源使用同一个中心登记器口径。
- 列表展示连接名称、类型、所属环境、运行客户端、执行位置、建立时间和最后活动时间。每条登记同时保存实际发起连接的环境上下文；App 还按活动连接 ID 在当前 Renderer 会话保存来源，以兼容尚未识别来源字段的旧 Endpoint，服务端返回的来源始终优先。点击记录时，环境详情发起的连接返回对应环境详情，全局 SSH、数据库、Redis 或 SFTP 工作台发起的连接分别返回对应工作台，不能用可能包含多个值的环境关联关系反推 SSH、数据库、Redis 或 SFTP 来源；Web 与日志固定归属单个环境，可在旧服务端数据中使用其环境关联。跨工作空间记录不会自动切换，因为现有工作空间切换会关闭活动连接。普通用户只能查看和关闭自己的连接；平台管理员可以查看和关闭全部用户连接。
- 当前用户从一个环境离开时，Renderer 把该环境加入最近离开优先的画中画栈；同时把当前用户、当前工作空间中其余能够定位到环境的活动连接按最近活动时间补入栈尾，因此已有连接不会因为未出现在本次导航栈而漏掉。进入另一环境时从可见集合中排除新前台环境，并在旧前台环境仍有活动连接时把旧环境放到栈顶。服务端返回的 `originEnvironmentId` 优先；当前 App 为旧 Endpoint 保存的活动连接来源其次；Web 与日志只有一个所属环境时可使用该关系推断。桌面本机 Web 创建时显式提交来源环境，登记完成后客户端立即刷新活动连接。环境页签变化通过路由查询持续记录；每张缩略图点击后优先恢复该环境最后访问的完整查询和已挂载工作区现场，没有记录时才使用最近活动连接生成 `tab + connectionId + activeConnectionId`，SFTP 额外携带 `mode=sftp`。卡片说明也优先选择与最后访问页签匹配的活动连接；关闭按钮批量调用既有活动连接关闭接口并立即隐藏已提交关闭的连接 ID。
- `EnvironmentWorkspaceHost` 在 App Shell 内持续挂载当前环境以及全部仍有可见活动连接的环境实例。前台实例直接进入正常布局，后台实例放在固定 `1280 × 720` 的离屏、不可交互且不可见容器中，只保留组件和连接现场，不参与缩略图绘制；环境详情从显式 `environmentId`、保存或按最近活动连接恢复的路由查询和 `active/preview` 状态读取上下文，不再依赖失活后脱离 DOM 的路由 `KeepAlive`。后台 SSH、Web、日志、数据库和 Redis 状态继续接收必要数据，但全局快捷键、Agent 场景、焦点和原生 Web 交互只属于前台实例。
- Renderer 只串行、限频地截取当前前台工作区，并通过全局路由前置守卫在离开环境或切换页签前保存最后一帧。普通 Web 使用当前可见工作区 DOM 生成 `640 × 360` JPEG；桌面 App 通过最小 IPC 让主进程对主 Renderer 的实际合成区域执行 `capturePage(rect)`，本机 Web 则继续由当前可见 `WebContentsView` 直接捕获真实页面。所有来源都按中心裁剪为 16:9 后降采样；后台原生 Web 视图隐藏并恢复 Chromium 默认后台节流，不再为画中画保持离屏绘制。缩略图只保存在当前 Renderer 和原生覆盖层内存中，不写入磁盘。
- macOS/Windows App 由主进程创建以主窗口为 parent、`focusable: false` 的透明无框 `BrowserWindow`，始终使用 `showInactive()` 展示，悬浮展开不会使主窗口失焦。窗口使用独立最小权限 preload 接收经过纯数据快照转换的画中画状态，并只回传展开、收起、环境切换、关闭和卡片拖动手势；只有明确打开环境或触发关闭确认时主 Renderer 才重新取得焦点。按住卡片任意非按钮区域时，主进程根据屏幕坐标直接移动子窗口，松手后把相对坐标回传 Renderer 持久化和吸附；短按仍执行环境切换。重叠态保留轻微层叠偏移，展开态所有卡片使用同一左坐标，展开、收起和环境交换保持第一张卡片屏幕锚点不变。子窗口保持在本机 `WebContentsView` 上方。静态桌面烟测验证 Renderer 与本机 Web 真实像素、变化帧、非激活悬浮、固定列和锚点、重叠/展开尺寸、预览不可交互、整卡拖动位置与关闭/打开动作回传、原生层级以及隐藏清理。
- 服务端执行资源在创建前向中心登记器预留额度，并由各运行时直接上报数据活动与关闭结果。macOS App 本机执行先使用执行实例标识预留额度，再建立本机资源；主进程每 3 秒上报活动时间和存活租约，并执行中心下发的用户或管理员关闭请求。
- 数据库与 Redis 工作台以用户选中并成功访问连接后建立的工作区作为逻辑会话计数；底层对象读取、查询和 Redis 命令继续按现有短连接或连接池执行。数据库逻辑会话被空闲回收、管理员关闭或异常中断时取消活动查询，但保留当前客户端中的 SQL、对象和数据页签并标记为已断开；用户在数据库工作台内主动关闭连接时，取消查询后立即清空该连接全部页签、对象树和选中状态。Redis 逻辑会话在成功测试、读取 INFO、扫描或执行命令后建立，工作台轮询中心状态并在连接切换、卸载、外部关闭或空闲回收时释放或清除本地会话标识。
- 连接被外部关闭后，当前客户端保留 Web 最后画面、SSH 终端输出、日志输出或数据库现场并标记为已断开，直到用户关闭对应工作区。退出登录、账号停用、权限撤销、Endpoint 切换和执行模式切换继续关闭相关连接。

### 4.5 连接资源池

- “全部”“待分配”“已分配”三个资源范围。
- 来源包括 SecureCRT 同步、SecureCRT 上传、Navicat 上传和手工创建。
- 支持搜索、原始目录筛选、来源筛选和连接类型筛选。
- 连接按连接组展示，支持逐组展开或折叠，并可一键展开或折叠当前筛选下的全部连接组。
- 支持多选连接后关联到一个或多个环境；批量操作会以所选环境集合替换原有关联。
- 同一条 SSH、数据库或 Redis 连接可以同时关联多个环境；没有任何环境关联的连接进入待分配队列。
- 支持取消全部环境关联、调整关联环境和批量删除。
- 最近一次连接巡检的可用性、耗时和错误摘要继续显示在连接列表。
- “数据处理”按钮进入连接工具页；该页面归属于连接资源池导航，不再占用独立主导航入口。

### 4.6 连接工具

- “连接同步”承载 SecureCRT SFTP 同步源、Cron、目录映射和同步冲突入口；同步产生的新连接继续进入连接资源池。
- “连接复制”只在组织工作空间向组织管理员开放。来源固定为当前管理员的个人空间，可多选独立 SSH/数据库连接、环境或环境组，并预览 Web 入口、Web 账号、日志和连接依赖。
- 复制时自动补齐 SSH 跳板机、数据库 SSH Tunnel 和日志依赖；同一源连接只生成一个组织目标并恢复与多个新环境的关联。密码、私钥和 Web 密码只在服务端解密并使用新随机 IV 重新加密，前端不取得明文。
- 目标组织存在同类资源时逐项选择“新建副本”或“复用”；不覆盖组织资源。复制完成时可按现有继承模型授权项目或成员，环境组授权持续覆盖以后加入该组的环境。
- 复制在一个元数据库事务中完成，任何资源、依赖、冲突映射、凭据或授权失败都会整批回滚。浏览器 Profile、页面状态、终端历史、SQL 历史、收藏和后台任务不复制。
- “连接巡检”承载 SSH/数据库/Redis 筛选、批量选择、真实认证、结果汇总和最近一次状态保存；Redis 使用认证后的 `PING`，资源池不再提供执行入口。

### 4.7 设置

- “个人信息”和“主题样式”固定为 Web、macOS 与 Windows 共享设置列表的前两项；桌面 App 的连接与客户端版本设置以及平台管理员的实例设置排在其后。旧“账号安全”不再作为独立分类。
- 个人信息只读展示用户名、平台角色、账号 ID 和注册时间，并集中提供修改密码和退出登录；侧栏用户名直接打开本页，不再弹出退出菜单。没有活动连接时直接退出，存在中心可检测的活动连接时先提示退出会关闭这些连接并要求确认。
- 主题使用 `light`、`dark`、`bright` 三值模型。选择保存在当前浏览器站点存储或桌面 App 本地存储中，不上传中心服务，也不按 Viron 用户隔离；同一客户端切换账号后继续沿用当前选择，不同浏览器 Profile 或 App 数据目录互不影响。旧 `light`/`dark` 值继续有效，缺失或未知值回退为浅色。
- 主题切换立即更新已经挂载的 Vue、xterm、Monaco 和数据网格，不刷新页面、不重建远端 Session，也不清空终端输出、日志、SQL/Redis 输入或数据库页签。
- 明亮主题以浅色应用外壳为基础，仅保留最外侧用户菜单栏为深色；连接列表、资源树、工具栏、SSH 终端与命令历史、SFTP、实时日志、数据库 SQL/结果/表数据/同步弹窗、Redis 命令与结果、普通页面和业务弹窗均使用浅色背景及适配浅底的 ANSI/语法色板。主题缩略图仍按其所代表的主题显示，嵌入网站和操作系统原生窗口不受影响。
- 平台管理员在“运行策略”中调整审计保留期和 10–3600 秒的宿主机监控采集频率；保存值进入 `settings` 表并直接被后台调度器读取，无需重启。`AUDIT_RETENTION_DAYS` 与 `VIRON_MONITOR_PULL_INTERVAL_SECONDS` 只提供尚无页面保存值时的默认值。

## 5. 环境与资源模型

工作空间使用 `workspace_type + workspace_id` 标识。个人资源的工作空间 ID 是用户 ID，组织资源的工作空间 ID 是组织 ID；环境组、环境、连接来源、连接组、SSH/数据库/Redis 连接和导入批次都保存归属。服务端列表、详情、导出、WebSocket 票据和后台任务均从登录会话读取当前工作空间，不接受前端直接声明资源范围。

环境组、环境、SSH 连接、数据库连接和 Redis 连接可以直接授权给组织成员或项目。环境组授权动态包含组内当前及以后新增的环境与关联连接，环境授权动态包含该环境当前及以后关联的连接；单连接授权不扩展到所属环境。普通成员的最终访问集合在请求时由直接授权与所有项目授权求并集。知识库节点授权独立表示编辑权，不会反向授予环境访问权；成员可在全局知识库查看组织文档，但只有能够访问环境时才能进入该环境的知识视图或增删该环境标签。

个人资源复制到组织时由当前组织管理员身份确定目标工作空间，服务端只读取该管理员自己的个人资源；客户端提交的资源 ID、自动依赖、复用目标和授权对象全部重新校验。

### 5.1 环境组

- 名称、描述、排序、颜色、创建/更新时间。
- 删除组时环境自动变为未分组，不级联删除环境。

### 5.2 环境

- 名称、简称、描述、状态、负责人、标签、排序、可选环境组。
- 状态：运行中、维护中、异常、已停用。
- 环境删除前需要确认；删除只解除该环境与连接的关联，连接没有其他环境关联时进入待分配队列，不删除连接和凭据。

### 5.3 Web 页面

本节的页面数据模型同时服务普通 Web 与 macOS App；涉及 Chrome Profile、画面流、上传下载和重新登录的执行行为描述普通 Web 的服务端 Chromium，App 当前行为见 15.3。

- 名称、URL、描述、标签、排序、多个账号；Web 入口和入口下账号分别维护服务端排序值，工作空间管理员拖动后事务更新完整顺序。
- 账号包含用户名、密码、备注和任意键值自定义字段。
- 密码默认隐藏；显示和复制会写审计。
- 选中入口和账号只创建待访问界面，不建立 Web 连接或请求目标地址；用户双击页面空白处或点击“访问页面”后才启动对应执行端并加载页面。
- “Viron 用户 + Web 凭据”对应一个独立 Chrome Profile，Cookie、缓存和 LocalStorage 不与其他账号共享。
- 服务端 Chromium 通过 WebSocket 提供页面画面和鼠标、键盘、滚轮、导航事件；用户界面保持类似嵌入页面的使用体验。
- 登录页位于入口原始 Origin 且表单识别可靠时自动填写并提交；验证码、MFA、跨域页面和不确定表单交给用户完成。
- 支持单账号视角切换、主动创建空白标签页、页面弹窗/子页面、上传下载、重新填充和重新登录；不同账号使用隔离 Profile，同一账号的多个页面共享 Profile。地址栏新建按钮在待访问状态也可用并负责建立当前账号 View 后创建空白页；仅当同账号存在多个页面时，入口与账号 Tab 下显示同风格的第三行页面 Tab，标签栏末尾同时提供新建入口。第三行页面 Tab 可在当前账号会话中拖动重排，普通 Web 的页面 Map 与 App 本机 `WebContentsView` Map 使用同一完整 ID 顺序校验；重排不切换活动页，关闭活动页后按当前顺序选择相邻页，账号页面全部关闭后不持久化页面顺序。全屏或沉浸模式自动附着同账号 View 并保留多页面标签。双视角入口在 Web 与桌面客户端统一隐藏。
- 浏览器目录在服务重启后保留；不可见账号页面停止画面流并在空闲后关闭进程，重新进入时恢复登录状态和最后地址。
- App 本机执行时，目标页面的原生右键菜单提供“检查元素”。主进程只对触发菜单的 `WebContentsView` 打开分离的 Chromium DevTools 并使用 Chromium 提供的坐标定位元素；Renderer 不接收 `webContentsId`、点击坐标、CDP 会话或任意脚本执行接口。DevTools 具备读取和修改当前页面 DOM、网络、Cookie 与存储的能力，属于当前获授权账号页面的显式本机调试入口；关闭页面、账号视图或 App 时对应调试会话随目标 `webContents` 一并销毁。

### 5.4 SSH 连接

- 名称、主机、端口、用户名、认证方式、字符集、终端类型、KeepAlive，以及用于标记 NACOS 等机器组件的自定义标签。
- 认证方式：密码、工作空间托管密钥、兼容的旧版内嵌私钥、keyboard-interactive、单级跳板机。
- `ssh_keys` 按 `workspace_type/workspace_id` 隔离，保存名称、算法、公钥、SHA-256 指纹和使用实例主密钥 AES-256-GCM 加密的私钥/口令；`ssh_connections.ssh_key_id` 只引用同工作空间密钥。
- 个人空间所有者和组织管理员可以导入外部私钥，生成 ED25519、RSA 3072 或 RSA 4096 密钥对，导出公钥/私钥、重命名和删除未使用密钥。普通组织成员不能列出或维护密钥，但可以使用已授权且引用组织密钥的连接。
- 可从已有连接打开预填表单并创建独立副本；源凭据不返回浏览器，由服务端解密后以新密文写入副本，表单填写的新凭据只覆盖对应字段。
- 可选跳板机引用另一条 SSH 连接，禁止循环引用。
- 可选登录脚本在交互式 Shell 建立后执行；脚本保存在连接选项中并受现有凭据、权限和审计边界约束。
- 保存 SecureCRT 原始路径、来源 ID、来源更新时间和解析后的附加设置。

### 5.5 数据库连接

- 名称、类型、主机、端口、用户名、密码、默认数据库、字符集、时区。
- 类型：MySQL、MariaDB。
- 可从已有连接打开预填表单并创建独立副本；源数据库密码、HTTP Tunnel 认证和 TLS 客户端材料只在服务端解密并以新密文写入，副本后续修改不影响源连接。
- 连接模式：TCP、SSH Tunnel、SSL/TLS、HTTP Tunnel；TCP/SSH Tunnel 可启用数据库 SSL。
- SSL 字段：CA、客户端证书、客户端私钥、私钥口令、是否验证服务器证书。
- HTTP Tunnel 字段：Navicat `ntunnel_mysql.php` URL、可选 HTTP Basic Auth、HTTPS 证书校验和请求超时。
- SSH Tunnel 引用资源池中的 SSH 连接。
- 密码、HTTP Tunnel 认证、TLS CA、客户端证书、客户端私钥和私钥口令只进入 `credential_ciphertext`；`options_json` 只保存 SSL 开关、证书校验、超时、Tunnel URL 和 SSH 连接 ID。服务启动会把旧版本残留在 `options_json` 的 TLS 材料幂等迁移到加密凭据，连接列表只返回 `hasTls*` 标记。

### 5.6 Redis 连接

- Redis 是与 SSH、数据库并列的一等连接资源，保存工作空间、连接组、多环境关联、主机、端口、可选 ACL 用户名、默认逻辑库、连接模式和非敏感选项。
- 当前执行拓扑是 Standalone；支持 TCP 直连、TCP/TLS、引用现有 Viron SSH 连接的 SSH Tunnel，以及 SSH Tunnel 上的 TLS。Sentinel、Cluster 和模块扩展仍属于后续能力。
- 密码、TLS CA、客户端证书、客户端私钥和私钥口令只进入 `credential_ciphertext`；`options_json` 只保存 TLS 开关、证书校验、服务器名称、超时、键分隔符、只读模式和 SSH 连接 ID。
- Redis 连接可独立复制；源凭据由服务端解密后使用新随机 IV 重新加密，前端列表只取得凭据存在标志。
- 连接巡检执行完整连接、TLS/SSH Tunnel、AUTH 与 `PING`，不会执行写命令。

### 5.7 环境日志配置

- 配置属于单个环境，并引用该环境内的一条可用 SSH 连接。
- 每条配置保存名称和 1–10 个绝对文件路径；旧版单路径配置启动时自动迁移到路径数组。
- 单个路径最长 1,024 个字符，不允许重复、NUL、回车或换行，不接受通配符、目录、任意 Shell 命令和额外参数。
- 日志正文只在活动 WebSocket 中传输，不写入元数据库、审计详情或浏览器持久存储。

### 5.8 工作空间知识库与环境视图

- `knowledge_nodes` 以 `workspace_type + workspace_id` 为归属保存 `folder` 或 `document` 节点，`parent_id` 支持任意层级，工作空间与 `parent_key + name` 保证同级名称不重复；文档名称统一使用 `.md` 后缀。`environment_id` 仅标记每个环境的专属存储根目录；普通文档和文件夹仍通过多环境标签关联。
- `knowledge_node_environments` 保存节点可同时关联的多个环境。文件夹的直接环境标签向全部后代继承；只有子文档匹配时，环境视图会把未打标签的祖先文件夹作为只用于定位的目录上下文展示。环境视图隐藏专属存储根目录并把其直接子节点映射为界面根级；环境内新建、导入或移回根级的节点实际保存到该目录并自动取得当前环境标签，移动节点保留直接标签并按新父链重新计算继承标签。通过标签关联的其他位置文档继续与专属目录内容一起展示。
- 文档正文直接保存在元数据库。编辑器支持编辑、分栏和预览；用户停止输入 1 秒后自动保存，同时保留保存按钮和 `Cmd/Ctrl+S`。保存请求携带 `revision`，服务端仅更新匹配版本；并发版本不一致返回冲突，客户端保留本地正文并提供复制和加载远端版本操作。
- `knowledge_node_grants` 只存在于组织空间，授权对象为普通成员或项目组。文件夹编辑权沿父链向后代继承，文档编辑权不扩展；普通成员对自己创建的文档始终拥有编辑、重命名、移动和删除权。创建文件夹及修改文件夹结构需要目标文件夹编辑权，根级文件夹只由管理员创建。
- `knowledge_assets` 按文档保存 Base64 图片，正文使用 `knowledge-asset://<id>` 引用。支持 PNG、JPEG、GIF 和 WebP，服务端同时校验扩展名、MIME 和真实文件签名；单图最大 30 MB，不接收 SVG 或原始 HTML。删除文档会级联清理专属图片。
- 单篇 `.md` 导入允许所有成员使用；ZIP 导入会恢复 Markdown 目录结构并把相对路径图片转为 Base64，创建目录结构需要目标文件夹编辑权。同名节点不覆盖，自动追加序号。全局导入不分配环境，环境内导入自动携带当前环境标签。
- 单篇文档导出为带 Data URI 图片的独立 `.md`；文件夹、整个工作空间知识库或环境筛选结果导出为保留目录结构的 ZIP，并把图片写入独立资源文件。
- 旧版按环境保存的节点在启动时迁入对应工作空间；每个旧环境创建同名顶层文件夹以规避跨环境同级重名，原有层级、正文、Base64 图片和编辑授权保留，迁移节点取得原环境的直接标签。

### 5.9 服务、部署节点与监控主机

- `services` 表示单个环境中的逻辑业务服务；同一服务可以关联多台机器上的多个 `service_deployments`，同一 SSH 连接也可以承载多个服务。`services.sort_order` 保存环境内服务清单顺序，`ssh_connection_environments.maintenance_sort_order` 保存该 SSH 连接在对应环境服务维护页中的宿主机顺序；两者都是工作空间共享配置，不改变连接资源池或其他工作台的排序。新建服务或新关联宿主机追加到当前顺序末尾，管理员提交完整 ID 集合后在事务中重排。部署节点始终引用当前环境已有的 SSH 连接，只保存关联和扫描标识，不复制主机或凭据。
- 部署节点类型为 systemd、Docker、Podman、Supervisor、Kubernetes 或普通进程。systemd、Docker、Podman 和 Supervisor 可以从扫描清单选择或手工登记，并提供由 Viron 固定生成的启动、停止、重启命令；Kubernetes 只能从已扫描的工作负载纳管，首版不提供启停或重启；普通进程只允许手工登记，不根据 PID 猜测通用启停方式。服务停用、SSH 连接删除或移出环境时拒绝维护动作。
- `service_script_actions` 保存服务级或部署节点级功能按钮。工作空间所有者和组织管理员设置名称、固定图标与最多 64 KiB 的脚本正文；`deployment_id` 为空时按钮作用于服务的全部部署节点，否则只作用于该部署节点。执行前重新校验环境与每条 SSH 连接权限，通过 `/bin/sh -s` 的标准输入发送正文，以 SSH 登录用户和默认工作目录运行，不自动提升权限。服务级动作最多四个节点并发，同一 SSH 连接承载多个部署节点时仍按节点分别执行；响应逐节点返回退出码、耗时和受限输出。审计不保存脚本正文、stdout 或 stderr，只保存按钮、目标、退出状态和不含远端输出的失败摘要。
- `viron-monitor` 只运行在 Linux 节点，默认每 30 秒启动受控采集子进程。采集器固定嵌入 Telegraf `v1.39.2` 的 CPU、内存、系统、磁盘、传感器、systemd、Docker/Podman 和 Supervisor 输入插件，并使用 Kubernetes 官方 `client-go v0.36.2` 解析 kubeconfig、建立认证客户端和读取资源；容器使用稳定名称作为维护标识，容器 ID、镜像和引擎地址保留在非敏感元数据中。rootful Podman 自动探测，其他 socket、Supervisor 地址和附加 kubeconfig 路径通过节点环境变量显式配置。
- Kubernetes 扫描分为发现与连接两阶段。节点检查 `$KUBECONFIG`、自身 `~/.kube/config`、`/etc/kubernetes/admin.conf`、K3s/RKE2 配置、`/root/.kube/config`、`/home/*/.kube/config` 和 `VIRON_MONITOR_KUBECONFIG_PATHS`，只把路径哈希、路径、cluster、context、默认 namespace、选择和连通状态传给中心，不传 kubeconfig 原文、证书、Token 或私钥。中心只允许选择最近样本中已发现的 context，并经 SSH 调用 `configure-kubernetes` 把 `sourceId + context` 写入节点数据目录；只有已选择的 context 才会解析认证配置或执行 kubeconfig 的认证插件并访问 Kubernetes API。
- 每个已选择 context 优先扫描全部 namespace；集群级列表被 RBAC 拒绝时回退到 context 默认 namespace。扫描 Deployment、StatefulSet、DaemonSet 的期望、就绪、可用、已更新和不可用副本数，按 generation 与副本状态归一化为运行、停止或异常；同时读取 Service，通过 namespace 和 selector 匹配后作为工作负载元数据，不把 Kubernetes Service 本身误当成运行进程。Kubernetes 外部标识是 kubeconfig 路径哈希、context、namespace、资源类型和名称的 SHA-256，不包含凭据。单轮 Kubernetes API 扫描总时限为 10 秒，最多四个 context 并发。
- 每份样本包含宿主机 CPU 总量及用户态/内核态/I/O wait/steal 分项、内存与 Swap、1/5/15 分钟负载、块设备吞吐与 IOPS、非回环网络吞吐/错误/丢包、CPU/内存/I/O PSI、磁盘挂载、可读取的温度、运行时间，kubeconfig/context 发现结果，以及扫描候选的状态和能够可靠归属的 CPU、内存、PID、启动时间、重启次数或 Kubernetes 副本数。采集器在同一受控采集窗口内读取 `/proc` 计数差值，并分别保留 CPU、内存和磁盘 I/O Top 5 的并集，最多 15 个进程；进程摘要只有 PID、名称、可执行文件 basename、运行用户、资源数值及能够通过主 PID 证明的工作负载身份，不采集命令行、参数、环境变量或文件内容。部署节点状态按 `provider + externalId` 匹配最近样本；扫描结果中不存在时标记为未知，不把 SSH 可连接等同于服务正常。
- 节点主程序以 SQLite/WAL 保存单调序列并在每次写入后删除超过 30 天的样本和缺口。默认缓冲上限为 256 MiB；超限时先把较旧原始样本聚合为分钟、小时分辨率，仍超限时删除最旧范围并生成 `local_buffer_limit` 序列缺口。`pull --after` 始终只读；兼容命令 `ack --through` 只记录确认游标，不再删除数据；`clear` 由用户显式触发，清空本地样本和缺口但保留 Agent 身份与下一序列。
- 服务维护对每条环境 SSH 连接提供一键安装。中心先经原 SSH/跳板链路预检 Linux、`amd64/arm64`、systemd、root/免密 sudo、服务端安装包和目标路径，再通过 SFTP 上传校验过 SHA-256 的同版本安装包到精确的 `/tmp/viron-monitor-install.*` 临时目录并执行安装；成功后关闭该连接的 SSH 池、重新登录、立即采集和拉取首批数据。页面和接口不接收 sudo 密码。
- 页面确认安装后创建中心后台任务，按预检、安装包校验、SSH 连接、临时目录、逐文件上传、远程安装、重连、首次采集和中心入库记录真实进度。任务状态和最多 300 条脱敏日志写入 `monitor_install_tasks`，关闭进度弹窗不终止任务，再次进入同一主机可以恢复查看；同一 SSH 连接只允许一个活动安装任务。失败保留发生阶段和错误，重试始终重新执行完整预检；服务重启时遗留的活动任务明确标记为中断，已结束任务保留 30 天。
- 默认程序目录为 `/opt/viron/monitor`，稳定命令入口为 `/usr/local/bin/viron-monitor`，配置为 `/etc/viron-monitor/viron-monitor.env`，数据为 `/var/lib/viron-monitor`，systemd 单元为 `/etc/systemd/system/viron-monitor.service`。自定义程序目录只允许 `/opt` 下无空白、无路径回退的规范绝对路径。安装清单匹配时视为原位升级；非空目录或非目录目标视为冲突；系统存在命令入口但目标目录没有有效 Viron 清单时视为旧手工安装并拒绝自动覆盖。临时上传目录在成功或失败后都尽力清理。
- 安装脚本必须由 root 执行，程序、配置和 systemd 单元由 root 持有，常驻主程序和受控采集子进程也固定以 root 运行，从而读取机器上的 root/普通用户 kubeconfig、systemd 和容器运行时信息；升级保留已有环境配置。`viron-monitor` 系统用户和同名组继续作为本地访问控制边界：节点数据目录由 `root:viron-monitor` 持有并使用 `2770`，指定的 Viron SSH 用户加入该组后可以执行 pull、clear、`configure-kubernetes`，以及连接 `/run/viron-monitor/control.sock`。安装器不复制凭据、不修改 kubeconfig 权限，也不在 Viron 保存 root 密码。root 采集能够接触宿主机敏感配置与容器 socket，必须视为高权限组件并通过 systemd 文件系统保护、固定二进制和最小本地接口约束其能力。
- 节点不监听网络端口，也不主动连接 Viron。root 守护进程只在 `root:viron-monitor`、`0660` 的本地 Unix Socket 接受带版本号的固定 `collect` 动作，不接收参数、Shell、路径或任意命令；周期采集和 Socket 触发采集在守护进程内串行执行并由其独占采集器与 SQLite 写入。中心服务通过现有服务端 SSH 命令执行器和配置中的单级跳板机运行普通 `viron-monitor collect`，该命令只等待 root 守护进程完成一次采集，随后以原 SSH 用户调用 `pull`。首次安装或从旧的非 root 服务迁移仍要求 root 登录或免密 sudo，日常采集、触发和拉取不再需要 root 凭据。缺少 `collectorUser: root` 或当前 `metricsVersion` 的旧样本即使产品版本相同也显示“更新监控服务”，更新只在用户确认后发生。每条新关联且尚无探测状态的 SSH 连接只自动探测一次；已识别 Agent 或 Viron 托管安装的节点按平台管理员配置的 10–3600 秒频率持续拉取，已确认未安装或连接失败的非托管连接不再周期性全量重试，每批最多四个工作空间 Agent 并发。
- 中心按“工作空间 + Agent”共享游标、最近快照和规范存储连接，同一工作空间内指向同一物理 Agent 的多条 SSH 连接只计为一台监控主机，启动归并旧重复样本和缺口；个人与组织工作空间仍分别保存中心数据。Agent 入库在进程内串行，主机记录按确定的连接主键顺序更新，MySQL 死锁只进行有限重试并在耗尽后明确失败。中心每次最多读取 20 个样本或缺口，SSH 输出上限为 16 MiB；批次超限时自动减半重试并记住该连接的安全批量，单条仍超限时明确报错。在同一元数据库事务内幂等写入样本、缺口、最新主机快照和部署状态，中心不再向节点发送删除确认，并固定保留最近 30 天中心样本和缺口。
- `GET /api/v1/environments/:environmentId/monitor-hosts/:connectionId/history?range=1h|6h|24h|7d|30d` 在环境和 SSH 权限校验后，按工作空间、Agent 与序列去重读取历史。响应覆盖全部宿主机指标、每个采样点的受限 Top 进程摘要，以及当前环境已纳管部署节点可归属的 CPU、内存、运行时间和重启次数；服务端保留首尾点并降采样到约 480 个绘图点，Agent 切换、显式缺口和异常时间间隔均标记为断档。分钟/小时压缩对连续数值和同一稳定进程身份做加权平均，进程重启后的 PID 变化不拆散能够通过工作负载或“用户 + 可执行文件”稳定识别的序列。响应同时返回当前、平均、P95、峰值、前后区间变化和按连续采样聚合的 CPU 饱和、I/O 等待、阻塞型高负载、内存压力、活跃换页、磁盘 I/O 压力、网络错误诊断区间。宿主机 CPU、内存和磁盘 I/O 使用区间固定 Top 5 加“其他进程”的堆叠组成，悬浮仍展示该点真实 Top 5；部署节点页面绘制 CPU、内存、运行时间和重启次数。环境组汇总尚未开放。
- 工作空间所有者或组织管理员为单个环境配置告警总开关，以及宿主机离线、CPU、内存、任一磁盘挂载点使用率、最高温度、磁盘挂载变化和部署节点停止/异常规则；各规则可以独立关闭。宿主机离线规则在中心 SSH 拉取失败、监控程序缺失、返回协议异常或最近样本超过其采集分辨率三倍且至少 60 秒未更新时记为异常；同一工作空间内按 Agent 去重，并沿用环境中的首选 SSH 连接作为跳转目标。中心对宿主机可用性检查和按样本时间入库的指标统一使用连续两次异常创建活动告警、连续两次正常恢复的状态机，活动期间不重复创建事件。磁盘以 `device + path` 为稳定身份；首次启用或重置规则时用最近有效快照静默建立基线，未知身份连续两次出现后创建 `disk_added` 一次性事件，既有身份连续两次缺失后创建 `disk_missing` 活动告警，随后连续两次重新出现后恢复。采集器先过滤常见虚拟文件系统，环境排除项不参与使用率或挂载变化判断。
- 服务可以关联当前环境的多个 `environment_logs`。服务维护只提供快捷入口，日志配置、SSH 路径、实时流和权限仍由日志模块管理。服务、宿主机清单排序、部署节点、日志关联和脚本按钮配置仍只允许工作空间所有者或组织管理员修改；有权访问环境和对应 SSH 连接的成员可以查看共享后的排序、监控、人工刷新、安装或更新监控服务、清理节点本地缓冲，以及执行固定的原生维护动作和管理员预先保存的脚本按钮。每个操作接口都重新校验环境和 SSH 连接权限。
- 服务维护页面激活时每 10 秒只读中心数据库并无感刷新，不由浏览器触发周期 SSH；全局客户端同时每 10 秒读取当前用户可见的告警和未读状态。每个用户对异常提醒、恢复提醒和已读状态独立持久化；离线期间产生且仍活动的告警在重新登录后提醒，一般已经恢复且从未提醒过的历史事件不弹恢复通知，但磁盘恢复挂载作为明确运维事件保留一次恢复通知。`disk_added` 使用 `event` 状态，不计入活动故障且不会产生伪恢复。站内通知始终可用并提供未读列表；桌面 App 通过受控 preload 请求 Electron 原生 macOS/Windows 通知，点击后只发送结构化环境/宿主机/服务 ID 回 Renderer；普通 Web 仅在浏览器 Notification API 可用且用户已授权时发送系统通知。扫描、拉取和原生动作始终由中心服务经服务端 SSH 执行，不跟随桌面 App 的本机直连/服务端转发设置；普通 Web、macOS 和 Windows 共享相同数据和操作语义，App 关闭不影响后台拉取。

## 6. 导入与同步

### 6.1 统一导入流水线

所有导入器输出统一的 `ImportedConnection`：

- 来源类型、来源 ID、原始路径、连接类型、名称、主机、端口。
- 用户名、加密凭据、认证与隧道设置。
- 原始配置摘要、解析告警、导入时间。

导入流程：

1. 获取或上传文件。
2. 在隔离临时目录解析。
3. 验证主机、端口、类型和加密字段。
4. 生成预览，展示新增、更新、冲突、失败数量。
5. 用户确认后写入连接资源池。
6. 使用目录映射自动关联环境，其余进入待分配。
7. 删除临时明文和上传文件。

### 6.2 脚本资源同步

- 个人空间所有者和组织管理员可以在当前空间创建脚本同步源，保存 `/bin/sh` 脚本、Cron 和同名资源处理策略。
- 脚本由不挂载 Viron 数据、数据库和主密钥的独立 Runner 容器串行执行；主服务只通过组权限 Unix Socket 提交任务。Runner 允许网络和 DNS，但限制 60 秒、1 CPU、256 MiB、128 个进程和 5 MiB 标准输出。
- 源码开发模式不依赖跨宿主共享 Unix Socket，而是通过 Docker CLI 为每轮创建使用同一镜像和限制的一次性容器；脚本仍只经 stdin 传入，容器结束后立即销毁。
- 标准输出使用 `schemaVersion: 1` JSON，覆盖环境组、环境、Web 入口/账号、SSH 密钥、SSH/数据库/Redis 连接及连接组、数据库配置档和环境日志。跨资源依赖用当前空间内名称解析。
- 名称按资源层级匹配；同步源选择 `ignore` 时保留同名资源，选择 `overwrite` 时覆盖全部明确匹配项。脚本未返回的空间资源只进入“空间额外”报告，不自动删除。
- 完整输出先校验再进入单一事务；依赖、私钥、凭据或写入失败时整批回滚。成功/失败均保留审阅报告，但原始输出、标准错误和明文凭据不落库。
- 同步创建或覆盖的 SSH、数据库、Redis 连接保存 `source_id` 并在资源池显示来源；其他业务资源只按名称匹配，不绑定同步源。

### 6.3 SecureCRT 同步源

当前本机“同步链接”脚本的行为是通过 SSH/SCP 将两个远端 SecureCRT 会话目录下载到本地。Viron 将其产品化：

- 页面配置同步源主机、端口、用户名、密码/私钥和多个远端目录。
- 支持立即同步和可选定时同步；定时同步默认关闭。
- 服务端使用 SSH/SFTP 拉取，不把同步凭据写入命令行或镜像。
- 保留 SecureCRT 文件夹树和会话原始路径。
- 解析 UTF-8 BOM 会话文件中的 Hostname、Port、Username、Protocol、Identity、Firewall/Jump、Password V2 等字段。
- 同步删除只表示来源已删除；已导入连接标记“来源已删除”，不直接删除 Viron 连接。
- 已关联连接同步更新时保留全部环境关联。

脚本迁移规则：同一 SSH 主机、用户名和认证凭据下的多个远端目录合并为一个同步源；脚本中的 `user@host:path` 分别映射为用户名、主机和远端目录。Viron 不执行脚本，不把密码传给 `sshpass` 或拼入命令行。首次同步产生的新增项进入待分配资源池，跨来源重复项进入冲突队列；管理员再通过目录前缀映射完成环境关联。

### 6.4 SecureCRT 文件导入

- 支持单个 INI、XML 设置导出和配置目录 ZIP。
- ZIP 限制大小、文件数、展开深度和压缩比，阻止路径穿越。
- `Password V2` 使用版本化解密适配器；无法识别时连接仍可导入，但标记需要补录密码。

### 6.5 Navicat 导入

- 首版支持 Navicat 15–17 导出的 NCX/XML，以及包含这些文件的 ZIP 包。
- 解析 MySQL/MariaDB 主机、端口、用户名、数据库、字符集、SSL、SSH Tunnel、HTTP Tunnel 和连接分组。
- 密码字段使用版本化适配器解密后立即重新加密。
- 未识别版本或加密格式明确标记失败，不写入错误密码。

### 6.6 去重与冲突

- 同一来源使用“来源 + 原始路径/来源 ID”作为同步更新主键。
- 不同来源出现相同协议、主机、端口和用户名时提示疑似重复。
- 用户可以合并、覆盖或保留两份；系统不自动跨来源合并。

## 7. SSH 与终端

### 7.1 连接链路

```text
普通 Web xterm.js ── WebSocket/一次性票据 ── Fastify SSH Gateway ── ssh2 ── 目标主机
macOS App xterm.js ── 受控 preload IPC/一次性票据 ── App 主进程 ── ssh2 ── 目标主机
                                                                        └── 跳板机 ── 目标主机
```

两端 Renderer 都只接收原始终端字节和脱敏控制消息，不获得主机密码或私钥。普通 Web 的凭据只在中心服务使用；App 主进程通过当前 Session 重新鉴权并解封 60 秒一次性 SSH 凭据信封，从当前 Mac 直接连接目标，不以服务端转发兜底。获授权的普通组织成员使用托管密钥连接时，私钥同样只进入其 App 主进程内存；本阶段接受成员从完全受其控制的本机进程提取私钥的风险，后续改用远程签名或受控 SSH Agent，使成员设备只提交签名请求。

### 7.2 会话生命周期

- 每个终端页签对应独立 SSH shell channel。
- 页面刷新、布局移动或短暂断开后可以使用会话票据重新附着。
- 普通 Web 由服务端、App 由主进程在 Renderer 分离后继续保存会话和最多 512 KB 输出缓冲。
- 默认连续 30 分钟没有 SSH 输入或输出后断开，由 `CONNECTION_IDLE_MINUTES` 调整。
- 用户主动关闭页签立即释放 shell 和连接。
- 同时最多 20 个会话。

### 7.3 终端能力

- xterm 终端渲染、UTF-8、256 色和真彩色；Web 端鼠标完成文本选择后自动复制，安全上下文优先使用 Clipboard API，普通 HTTP 页面使用兼容复制回退。Web 端不注册右键点击粘贴动作，复制失败时显示明确提示，粘贴继续使用 xterm 的 `Ctrl/Cmd+V` 键盘路径。
- 窗口 Resize 同步到远端 PTY。
- 重连、清屏、全屏、字体大小、主题、KeepAlive。
- 单窗、左右、上下、四宫格预设。
- 会话页签可拖入不同分屏区域，分隔条可拖动。
- 环境级保存页签和布局；只保存连接引用，不保存正在运行的命令。
- 可为连接启用多行登录脚本；交互式 Shell 就绪后自动写入，换行统一为 LF，并保证结尾换行。
- 最近 50 条命令按 Viron 用户和 SSH 连接保存在浏览器本地；记录命令、时间和可识别的远程路径，明显包含密码、令牌或密钥赋值的命令不保存，连续重复命令不重复追加。
- 用户输入时在终端光标附近展示历史命令建议，前缀匹配优先于其余文本匹配；支持鼠标、方向键和 `Enter`/`Tab` 选择，选择后只替换当前输入而不执行。输入辅助默认开启，其开关按 Viron 用户保存在当前浏览器，不影响历史面板和已有历史。
- ZMODEM 检测器在远端执行 `rz` 时提供本地文件多选上传，在执行 `sz` 时接收并保存文件；传输期间原始字节不经过 UTF-8 JSON 转换。

### 7.4 SFTP

- SFTP 集成在 SSH 工作台内，通过终端工具栏打开右侧抽屉，不设置全局主导航或环境工作区的独立入口。抽屉的最大范围限制在终端内容区内，不覆盖左侧服务器列表，并在桌面视口始终露出一部分 SSH 终端；收起抽屉不会关闭已打开的 SSH 会话或中断当前执行端的传输任务。
- 双栏分别维护主机、路径和选中项；主机按钮用于切换连接，路径支持面包屑导航与双击手动输入，文件筛选和上传、下载、新建目录、重命名、删除、显示隐藏文件、chmod 等操作集中在栏内工具区与“操作”菜单。
- 工作台固定为左右两栏，每栏独立保存 SSH 连接、当前目录、文件列表和单个选中项，并持续显示连接名称、登录用户、主机、端口和路径。
- 从环境进入时，主机选择器把当前环境关联的 SSH 连接作为推荐组优先展示，但不以环境关联关系过滤候选项；当前工作空间内其他有权访问的 SSH 连接仍可选择，实际使用要求普通 Web 的中心服务或 App 所在 Mac 能到达目标网络。每组内已经打开且仍有效的 SSH 会话连接优先排列并标记为活动；从全局 SSH 工作台进入时展示当前工作空间内全部有权访问的 SSH 连接。
- 两栏均支持进入目录、返回、刷新、上传、下载、创建目录、重命名、删除，以及查看和修改 Unix 权限；普通 Web 上传展示浏览器到服务端的进度，App 按 256 KB 分片经 IPC 写入主进程，下载由对应执行端触发保存反馈。
- 用户可把一侧选中的文件或目录复制到另一侧当前目录。开始前当前执行端递归检查来源规模和目标同名项，确认页固定展示来源主机、来源路径、目标主机和目标路径；存在冲突时用户明确选择覆盖或跳过。
- 普通 Web 由 Viron 服务端、App 由本机主进程同时建立两个独立 SFTP 连接并流式中转，不向 Renderer 或目标主机分发另一端凭据。目录递归传输时覆盖模式合并目录并替换同名文件，跳过模式合并目录但保留同名文件；符号链接、根目录传输、同一路径复制及把目录复制到自身子目录均被拒绝。
- SFTP 传输管理器每实例最多同时运行 3 个任务，展示字节/文件进度、平均速度、完成、跳过和失败原因，支持取消运行中任务及重试失败或已取消任务。任务保存在当前服务进程或 App 进程内，最多保留最近 100 个已结束任务；执行端重启会中断活动传输，不提供断点续传。
- 中心服务在签发每个 App SSH 凭据信封前重新校验 Session、工作空间和连接权限；App IPC 只接收连接 ID、规范化远端路径、文件选择结果和受限操作，主进程再次校验任务归属。所有失败向用户显示可操作原因；完整本机个人/组织操作审计仍属于审计安全收口阶段。

### 7.5 环境日志

- 服务端通过现有 SSH 认证、跳板机、指纹和超时能力建立独立通道；连接未启用登录脚本时直接使用 exec，启用且脚本非空时申请 PTY Shell，先写入登录脚本，再在同一交互环境写入受控 `tail` 命令，以支持脚本进入容器或切换运行用户的场景。
- 远端命令固定为 `tail -n <line-limit> -F -- <quoted-path>...`；行数上限只允许 1–5,000 的整数，同时控制初始读取行数和当前展示窗口，每个路径分别按 POSIX Shell 单引号规则转义，用户不能更换命令或参数。
- 多文件输出按 `tail` 到达顺序展示，`tail` 切换来源时输出文件头；不额外解析、排序或持久化正文。
- 页面支持配置列表折叠和宽度拖拽、单击选择、双击或回车启动、独立停止/重新连接、清屏和自动滚动；每个配置保存独立的连接状态和最多 5,000 行客户端输出，切换配置、环境工作区页签、菜单或路由不会停止其他活动流。用户可把行数上限设置为 1–5,000 行；日志新增会刷新空闲时间；用户主动停止、统一连接管理关闭、空闲超时或远端结束时释放对应通道。
- 页面默认按 Grafana 常用级别语义区分 critical/fatal、error、warning、info、debug 和 trace，并高亮时间戳、IP、常见请求/消息标识、耗时和 HTTP 状态字段；错误堆栈续行继承错误级别。关键字过滤在当前展示窗口内执行固定字符串匹配，可选择区分大小写，并在上文、下文或上下文模式中设置 0–200 行上下文，匹配词独立高亮。高亮只作用于 HTML 转义后的客户端显示，可关闭语义高亮且不改变原始文本。
- 下载导出当前屏幕文本；启用关键字过滤时导出过滤结果，未过滤时导出当前展示窗口。普通文件 `tail` 没有与 `kubectl logs --since/--since-time` 等价的时间查询语义；当前不猜测日志格式，不做时间过滤、按日志时间排序、级别过滤或历史保存。

### 7.6 不支持的 SSH 能力

- 不开放任意本地、远程、动态端口转发。
- 数据库 SSH Tunnel 只在服务内部存在，不监听公共端口。
- 不支持宿主机 SSH Agent 转发。

## 8. 数据库工作台

### 8.1 连接与对象树

- 连接测试、重新连接、连接状态和错误详情。
- HTTP Tunnel 兼容 Navicat `ntunnel_mysql.php` 二进制协议；目标端必须已有兼容脚本。
- HTTP Tunnel 是请求级连接，事务语义受该协议限制；要求严格长事务时使用 TCP 或 SSH Tunnel。
- 数据库工作台采用与 Navicat 一致的单一导航窗格：连接组、连接、数据库、对象分类和具体对象位于同一棵树中，主工作区不再额外占用独立对象栏。导航窗格可隐藏和拖拽调整宽度；右侧信息窗格独立显示常规属性及 DDL/预览，也可隐藏和调整宽度。
- 连接行单击只更新当前选中行，双击或回车才建立连接；建立中的状态、已连接状态和普通选中状态分开显示。主动关闭连接时清空对应行选中、数据库树、对象选中和页签，空闲回收或异常断开则保留编辑内容并显示重新连接入口。
- 对象列表按 Navicat 的 MySQL 命名显示“表、视图、函数、事件、查询、备份”；数据库和全部分类使用独立展开箭头，单击数据库名称只选中，双击名称或单击箭头才打开或关闭。存储过程和函数统一归入“函数”，触发器仍可由 DDL、备份恢复和迁移能力处理，但不作为顶级导航分类展示。查询节点按数据库显示 SQL 收藏，备份节点显示该连接、该数据库的备份任务和实时进度。
- 数据库、分类和具体对象节点使用同一套 Navicat 顺序右键菜单。右键先选中当前节点；分类节点只启用无需对象选择的命令，具体对象节点再启用打开、设计、删除、复制、重命名和表维护等命令。菜单支持分隔、悬停或方向键展开任意层级子菜单、`Escape` 关闭和视口边缘避让；表维护中的分析、检查、优化、修复和行数统计生成可审阅的受控 SQL。已有 DDL、表导入导出、SQL 文件恢复、数据库备份、任务面板和跨分类对象查找共用原执行路径。没有完整执行路径或尚未达到交付标准的产品能力不展示入口；已经实现但因未连接、未选对象、任务状态或权限不足而暂不可执行的动作保留并置灰，同时说明原因。
- 数据库和基础数据表可以按用户收藏；收藏区位于统一导航树顶部，使用对象名与“连接 · 数据库”的紧凑两行结构并显示收藏数量。打开收藏时自动建立或恢复目标连接并展开、滚动定位对象。数据库收藏打开表对象页，数据表收藏直接打开对应数据页；“仅显示已标星”保留连接自身已标星或包含对象收藏的连接。收藏与 SQL 收藏使用独立数据模型。
- 顶部全局工具栏按 Navicat 顺序提供连接、新建查询、表、视图、函数、用户、事件、查询、备份及窗格开关。连接主按钮直接进入新建连接，独立箭头打开连接菜单。尚未完成完整交付的自动运行、模型和 BI 不展示入口。Viron 的 SQL 收藏、执行历史、任务面板、备份恢复、数据传输、数据同步和结构同步位于窗格开关之后的独立 `Viron` 扩展区，不插入 Navicat 原生按钮序列。
- 表、视图、函数和事件打开统一的对象明细页；查询和备份也使用对象列表页，提供 Navicat 对应列、搜索、新建和刷新操作，不再只通过侧栏或任务抽屉访问。
- 刷新对象、搜索对象、查看属性和生成 DDL；`Cmd/Ctrl+F` 在对象或查询/备份页聚焦当前列表搜索，在表数据页打开筛选，在 SQL 编辑器内使用 Monaco 查找，在其余上下文聚焦统一导航搜索。数据库右键菜单另提供跨分类“在数据库中查找”。
- 通过 DDL 页签查看、修改、新建、重命名和删除相应对象。
- 连接配置文件使用真实独立加密数据库连接记录并继承主要连接授权。连接编辑窗口提供新建、设为活动、复制和删除，右键菜单只负责切换；当前用户打开主要连接或任一配置文件时，创建、切换、编辑、复制、删除和设为活动均拒绝执行。双击主要连接时默认使用活动配置文件，显式“通过配置文件打开表”不改写其他配置。
- 数据库用户页支持创建、编辑、删除和权限管理员；表、视图、函数和过程的右键“设置权限”进入对象授权工作流。

### 8.2 SQL 编辑器

- Monaco Editor，多查询页签；查询工具栏按 Navicat 顺序提供保存、SQL 美化、代码段、结果上下/左右布局、专注模式、连接与数据库选择、运行、解释和执行期间出现的停止按钮。尚未完成关系画布、可视化 Join 和嵌套子查询工作流的查询创建工具不展示入口。运行主按钮直接执行当前选择或整个编辑器，箭头菜单提供运行、运行当前语句、运行已选择的和遇到错误时继续；执行历史只从 `Viron` 扩展菜单进入，不占用 Navicat 查询工具栏位置。
- MySQL 语法高亮和 Navicat 风格上下文补全。补全元数据按当前连接及数据库从 `information_schema.COLUMNS`、`TABLES` 和 `ROUTINES` 读取并缓存，数据库对象刷新或表结构保存后失效重载；桌面本机模式直接从本机数据库读取，不回退中心服务。
- 普通输入同时提供 SQL 关键字、数据库、当前库表/视图、函数、存储过程和查询参数；`FROM`、`JOIN`、`UPDATE`、`INTO` 等对象位置优先提供表和视图；识别当前语句内的表别名，输入 `别名.` 或 `表名.` 时只返回对应字段，并显示字段类型及完整来源。别名定义可以位于当前光标后方。
- 格式化 SQL。
- 执行当前语句、选中语句或全部脚本。
- 运行菜单支持“遇到错误时继续”；开启后使用支持注释、引号、Viron 备份边界及 `DELIMITER` 的语句解析逐条执行，失败语句作为独立结果保留并继续后续语句，最终任务保持失败状态以进入审计和历史错误统计。
- `EXPLAIN` 当前语句。
- 取消正在执行的查询。
- 查询历史、搜索、按连接和数据库归属的收藏、重新打开。
- Web 默认使用 `Cmd/Ctrl+F/N/W/S/R/Enter` 执行搜索、新建、关闭当前页签、保存或提交、刷新和执行。桌面 App 从主进程拦截这些业务快捷键，避免 Electron Renderer 触发网页刷新或关闭窗口；用户可在“设置 → 快捷键”录制、清除、恢复和保存当前设备的绑定，冲突或系统保留组合不能保存。`Cmd/Ctrl+Shift+W` 固定关闭窗口，不参与自定义。表数据页的保存表示提交变更，SQL 收藏继续从独立 `Viron` 扩展菜单进入，不能占用查询保存语义。
- 显示执行时间、影响行数、警告和错误位置。
- 同时最多 10 个查询。

### 8.3 数据网格

- 服务端分页、排序和筛选。
- 列宽调整、列顺序调整、隐藏列和复制单元格/行；默认不固定任何业务列。
- 主键或唯一键存在时支持单元格直接编辑。
- 批量新增、修改、删除和提交/回滚变更。
- 没有唯一键时禁止直接编辑并说明原因。
- CSV、XLSX 数据导入；CSV、XLSX、SQL 数据导出。
- 大导出转后台任务并提供下载。
- 数据加载期间“停止”按钮可用；Web 使用 `AbortController` 取消请求，桌面本机模式同时使迟到响应失效，停止后旧结果不能覆盖当前页。
- 表配置文件保存在 Viron 自身的 `database_table_profiles`，按用户、连接、数据库和表隔离，不写入目标业务库。配置记录当前筛选、排序、列顺序、列宽、隐藏列、分页大小和网格/表单模式，并提供管理、加载、保存、另存为、重命名和删除；加载时忽略已删除字段并自动补入新字段。

### 8.4 结构与对象维护

- 表、视图、存储过程、函数和事件可从对象列表查看 `SHOW CREATE` 结果；触发器由 DDL、备份恢复和迁移流程继续支持。
- 新建表进入独立可视化设计器，不要求用户手写建表 SQL。设计器按 Navicat 提供保存、添加、插入、删除和专注模式，以及字段、索引、外键、触发器、检查、选项、注释和 SQL 预览页；字段和索引具有独立属性区，触发器具有源代码区。首次保存时询问表名，空白表选项不写入 DDL；保存前校验名称、字段引用、生成列、自动递增、约束和触发器配置，通过现有受控查询任务执行生成的 MySQL DDL，成功后刷新对象树并打开新表数据页。
- 字段高级属性包含填充零、字段字符集/排序规则、二进制、列格式、存储、生成列和主键前缀长度；索引包含字段级前缀长度/排序、方法、基数、压缩、键块大小、解析器和可见性；表选项包含表空间、数据/索引目录、延迟键写入、封装键、校验和、加密、联合表、插入方法、统计持久化/自动重计/样本页和事务属性。联合表、标识符、解析器和原始分区表达式在生成 DDL 前执行约束校验。
- 已有表通过表设计接口读取字段、索引、外键、触发器、检查、完整高级选项和注释并进入同一可视化设计器，保存时生成并执行受控结构差异；主键前缀变化会重建主键，索引字段级属性变化会重建索引，支持将可恢复默认值的高级选项还原为服务器默认。Web 服务端和桌面本机执行层提供相同的表设计读取契约。视图、函数、过程和事件继续使用完整 DDL 编辑，避免丢失数据库特有定义。

### 8.5 备份与恢复

- 使用 `mysql2` 读取结构与数据并生成带 Viron 语句边界的 SQL 备份。
- 备份包含表与数据、视图、存储过程、函数、触发器和事件。
- 备份输出到 `/data/backups/database`。
- SQL 文件上传恢复，显示后台进度、日志和错误。
- 支持取消仍在执行的任务。
- 对象树的“备份”节点显示当前连接和数据库的任务及进度，任务面板轮询结果同步回对象树。
- 备份对象支持删除、复制、重命名、还原、提取 SQL 和 Finder 定位；完整与仅结构备份分别保留生命周期和输出文件。

### 8.6 一次性数据传输

- 源和目标均为 Viron 数据库连接。
- 选择源/目标数据库，可选表范围。
- 仅结构、仅数据或结构+数据，并可包含视图、存储过程、函数、触发器和事件。
- 可选择覆盖同名对象；未覆盖时由数据库错误停止任务。
- 数据按批次写入，展示对象级进度、行数和失败详情。
- 不提供定时和双向同步。

## 9. Redis 工作台

### 9.1 执行边界

- 普通 Web 由 Fastify 内的 Redis 运行层执行；桌面 App 本机模式由 Electron 主进程执行。Renderer 只提交连接 ID、逻辑库、结构化命令和二进制参数，不接收密码、TLS 私钥或 SSH 凭据。
- App 按连接取得绑定请求 ID、设备、用户、工作空间、Endpoint、目标地址、连接模式和资源版本且 60 秒有效的一次性 RSA-OAEP-SHA256 + AES-256-GCM 凭据信封。SSH Tunnel 的主连接与单级跳板机版本同时进入受保护声明。
- 两个执行端共用 `src/shared/redis.ts` 的命令分类、资源限制、二进制响应和 INFO 解析。未知命令默认拒绝；只读连接在可信执行端拒绝所有写命令。
- 当前每次测试、扫描、信息读取或命令请求使用一个专用短连接，执行结束立即关闭 Redis 与 SSH Tunnel；连接配置、工作空间、Endpoint、用户或执行模式变化时关闭仍在执行的受影响请求。本机失败不回退到服务端。
- 桌面 App 的本机测试、扫描、诊断和命令结果通过设备 RSA-PSS-SHA256 签名报告回中心，中心重新校验权限并幂等写审计。当前短连接模式不支持中心断线后继续新操作；离线审计队列和长会话属于路线图事项。

### 9.2 键空间与数据维护

- 键列表只调用 `SCAN`，每批 `COUNT` 提示值允许 10–1000；界面自动续传游标直到遍历完成或达到 10,000 个键的保留上限，服务端为每批键补取 `TYPE` 与 `PTTL`。界面对同一遍历返回的键去重，并明确说明游标遍历不是一致性快照。
- 支持 glob 和类型筛选，以及按连接分隔符的列表/一级分组视图。Redis 游标不换算成虚假百分比。
- String 使用 `GET/SET KEEPTTL`；Hash、Set 与 Sorted Set 使用增量扫描和对应增删命令；List 最多预览 500 项并提供两端 push/pop；Stream 最多预览 500 条 entry 并提供 `XADD/XDEL`。结构化修改采用 Redis 实际的最后写入语义，界面不承诺通用版本 CAS。
- 支持新建六种核心类型、`COPY`、`RENAME`、`UNLINK`、相对 TTL 和 `PERSIST`。大键删除优先 `UNLINK`，不提供 `KEYS`、Flush、脚本、配置、ACL、持久化、复制或拓扑修改入口。
- 键和值响应保存原始 Base64，并提供 UTF-8、JSON、Hex 与 Base64 解释；非 UTF-8 内容不会被损坏或伪装成文本。单个命令最多 256 个参数，文本参数最多 256 KiB、Base64 参数最多 512 KiB，响应硬上限 2 MiB。

### 9.3 命令与诊断

- 命令工作台使用 Viron 自己的 Monaco 组件，支持命令高亮、基线补全、选中/全文执行、最多 20 行顺序批量执行、最近结果和本地收藏；多行执行不承诺原子性。
- 安全策略区分读、写和拒绝；`KEYS`、`FLUSH*`、`CONFIG`、`ACL`、`EVAL`、`SCRIPT`、`DEBUG`、`MONITOR`、订阅及未知命令拒绝。集合范围、`SLOWLOG GET`、多键读写和删除具有独立数量上限，拒绝尝试也进入审计。
- `INFO` 展示当前连接节点的版本、模式、运行时间、内存、客户端和当前逻辑库统计；`SLOWLOG GET` 最多读取 1,000 条，UI 默认读取 32 条，并标明它是服务端执行耗时而非端到端延迟。
- `ioredis` 的连接与命令超时默认都是 10 秒，可在连接选项中设置 1–120 秒；离线队列、自动重试和请求重放关闭。Redis 响应使用 Buffer 路径，数字使用字符串表示，避免大整数精度丢失。

## 10. 审计与保留

审计事件包括：

- 登录、退出、密码修改和 CLI 密码重置。
- 注册、平台账号创建/停用/重置、工作空间切换、组织和成员变更、项目成员变更、资源授权与撤销。
- 环境、分组、Web 页面、连接和同步源的增删改，以及 SSH/数据库/Redis 连接复制。
- 凭据显示、复制和重新保存。
- 导入、同步、冲突处理和环境分配。
- SSH 连接、重连、断开和终端录像索引。
- 连接批量巡检及其结果。
- Redis 连接配置、复制、凭据信封发放、测试、扫描、INFO、命令执行、命令失败和策略拒绝；审计保存命令名、参数数量、执行位置、耗时与响应字节，不保存完整值或响应。
- 环境日志配置的增删改、开始查看和停止查看；日志正文不进入审计详情。
- 服务的创建、修改和删除，部署节点与日志关联变更，监控服务安装/更新及失败、监控主机人工刷新，以及部署节点启动、停止、重启的成功或失败；审计保存动作、目标、路径、版本、架构、耗时、退出状态和脱敏错误，不保存监控原始载荷、安装包内容或远端命令输出。
- SFTP 上传、下载、重命名、删除和权限修改。
- SQL 文本、连接、数据库、耗时、影响行数、成功/失败。
- 备份、恢复和数据传输任务。
- 平台迁移包导出与导入暂存。

操作事件、终端录像与 SQL 历史共用审计保留策略，默认保留 30 天。服务启动及周期清理会删除超过配置期限的记录和录像文件；列表按保留期查询并分页传输，不以固定条数截断可查询总量。三个页签均支持按操作者精确过滤和非敏感可见描述字段的关键字模糊匹配，SQL 正文与审计详情 JSON 不进入关键字搜索。

## 11. 安全设计

### 11.1 凭据加密

- 使用 Node.js `crypto` 的 AES-256-GCM。
- 每个秘密使用独立随机 96-bit IV 和认证标签。
- 显式配置 `VIRON_MASTER_KEY` 或 `VIRON_MASTER_KEY_FILE` 时优先使用外部主密钥；未显式配置时，首次启动自动生成 32 字节主密钥并以 `0600` 权限保存到 `DATA_DIR/master-key`，容器重建、服务重启和升级继续复用。任何 `ENVMAN_*` 环境变量都不会兼容读取。
- 自动生成前检查注册到主密钥验证器的全部密文列，包括 Web、SSH 连接、SSH 密钥库、数据库、Redis 和邀请令牌；已有加密数据但托管密钥缺失时拒绝启动。已有密钥与任一现存密文不匹配、文件不可读或权限宽于 `0600` 时也拒绝启动。
- 元数据库只保存版本、IV、认证标签和密文。
- 明文只在一次连接或导入任务的内存中出现。
- 日志、API 响应、WebSocket URL 和浏览器存储不得包含明文凭据。
- 平台迁移包使用 `scrypt` 派生 256 位包装密钥，并以 AES-256-GCM 加密来源主密钥；迁移 ID 进入附加认证数据。导入时来源密钥只在内存中短暂存在，快照内全部密文随后用目标实例主密钥重新加密。

### 11.2 Web 安全

- 所有平台用户密码使用 Argon2id；用户名按大小写不敏感方式保持唯一。
- 登录会话使用 HttpOnly、SameSite=Strict Cookie；HTTPS 部署通过 `COOKIE_SECURE=true` 启用 Secure。
- Helmet/CSP 和仅作用于 `/api` 的速率限制；自主注册额外限制为同来源每小时最多 10 次。首版不单独签发 CSRF Token，依赖 Strict Cookie 与同源 API。
- WebSocket 使用短期一次性票据并校验登录会话。
- 用户停用、移出组织、项目移除、授权撤销或工作空间切换时，服务端立即关闭受影响的 SSH、日志、数据库任务和 Web 页面运行上下文。
- 所有上传文件限制大小、类型和路径。
- 目标主机由个人所有者或组织管理员配置；首版不内置 CIDR 白名单，生产部署应通过容器网络和出口防火墙限制可达网段。

### 11.3 数据库查询

首版不做审批或 SQL 风险拦截。获授权用户使用数据库连接时可以修改数据和结构，但仍提供：

- 查询取消、连接超时和结果分页。
- 单次响应大小限制，避免服务进程内存耗尽。
- 所有 SQL 审计。

### 11.4 MCP 安全边界

- 远程 `/mcp` 只接受绑定具体用户的有效个人 API Key；平台 API Key、登录 Cookie 和匿名请求不能初始化 MCP Session。MCP Session 同时绑定 API Key、用户和随机执行 Scope，断开、空闲超时、Key 撤销或服务关闭时回收其 Web、SSH、SFTP、日志、数据库任务和未完成 Operation。
- 桌面 `viron-mcp` 是随 App 安装的 STDIO 协议适配器，不持有业务状态或连接秘密。它从 App 用户数据目录读取权限为 `0600` 的 Broker 描述文件，通过当前操作系统用户可访问的 Unix Domain Socket 或 Windows Named Pipe 完成随机 Token 握手；Electron Renderer 不接触 Broker、设备私钥或 MCP 请求。
- MCP 对客户端固定暴露上下文、领域大纲、操作搜索、单操作 Schema、四类分级执行器和 Operation 生命周期共 11 个网关工具。原专用工具与版本化业务白名单统一作为内部 operation 目录；搜索只返回摘要和输入字段摘要，完整 JSON Schema 仅按单个 operation 读取。工具解析继续经过统一禁止策略，拒绝账号、会话、API Key、组织成员、项目成员、资源授权、数据库账号权限、桌面设备内部接口、秘密显示和私钥导出等路径，不能提交任意内部 URL、IPC 或 CDP method。
- 连接和 Web 账号结果只包含非敏感元数据与秘密存在标记。数据库和 Redis TLS 材料与密码统一加密保存；MCP 响应、Resource、Operation、错误和审计不包含密码、私钥、Cookie、Token、证书私钥、文件内容、终端输出或查询结果的非必要副本。
- 凭据新增或更新通过 10 分钟短时单次 Operation 完成。远程模式要求用户使用同一 Viron 账号和工作空间打开安全页面；该页面拒绝 API Key 认证。桌面模式由 App 打开同 Endpoint、同 Session 分区的沙箱化安全窗口。秘密从表单直接进入 Viron 加密存储，不写入工具参数、URL 或 Operation Record；凭据 Operation 永不受自动审批策略影响。
- 风险业务 Operation 使用 `always`、`high-risk`、`never` 三档审批策略。远程模式把策略保存在个人 API Key 的 `mcp_approval_mode`，本机模式保存在 App 设备状态并通过 `X-Viron-MCP-Approval-Mode` 传递。`always` 确认全部风险 Operation，`high-risk` 仅确认高风险 Operation，`never` 自动执行全部风险 Operation；已有记录和缺失值都按 `always` 处理。
- SSH 命令、数据库/Redis 写入、SFTP 修改与传输、Web 提交/导航/上传、恢复、同步和其他风险业务操作始终先创建 Operation。只有审批策略判定需要人工确认时，confirmation Operation 才进入 `awaiting_purpose`，返回 `viron_operation_purpose` 下一步而不暴露审批页地址；Agent 仅提交 `operationId` 与 8–80 字的一句话意图后，Operation 才转为 `pending` 并允许打开审批页。purpose 写入 Operation 与审计，但页面明确标注其来自 Agent，真实命令或操作内容仍是审批依据。自动审批和低风险查询不生成 purpose，由服务端或 App 领取不可重放的一次性租约并立即按当前模式执行。Codex 批准是独立客户端层，不替代 Viron 权限校验，失败不跨执行模式回退。

## 12. 技术架构

### 12.1 总体结构

中心业务仍由一个 Node.js 进程和一个 Lite/Full 主服务镜像承担；远程 MCP 是该主服务内的独立 Fastify 模块，本机 MCP 通过桌面 App Broker 复用同一业务边界；另有无业务数据的脚本 Runner 作为安全边界。可选的 `viron-monitor` 安装在 Linux 目标节点，本地缓冲观测数据并由中心反向拉取：

```text
Codex ── Streamable HTTP `/mcp` ─┐
                                 │
Vue 3 SPA ───────────────────────┤ HTTP / WebSocket
                                 │
Fastify API + Static + MCP ──────┤
  ├─ Auth                │
  ├─ Resource API        │
  ├─ Import Worker       │
  ├─ SSH/SFTP Gateway    │
  ├─ MySQL Gateway       │
  ├─ Redis Gateway       │
  ├─ Service Monitor Puller
  └─ Audit/Cleanup       │
                                 ├── Unix Socket ── Script Runner ── /bin/sh + 网络/DNS
                                 ├── SSH/跳板链路 ── Linux 节点
                                 │                   ├─ viron-monitor ── SQLite/WAL
                                 │                   └─ viron-monitor-collector ── Telegraf 输入插件
                                 ▼
                    /data/envman.db 或 MySQL/MariaDB
                    /data/recordings
                    /data/web-profiles
                    /data/web-downloads
                    /data/backups/database
                    /data/uploads

Codex ── STDIO `viron-mcp` ── Unix Socket / Named Pipe ── Viron App 主进程
                                                        ├─ 本机 Web/SSH/SFTP/日志/数据库/Redis Runtime
                                                        └─ 当前 Endpoint 服务端转发桥
```

当前单实例容量不需要把 Redis 作为 Viron 自身的元数据或任务基础设施，也不需要 PostgreSQL 或独立任务服务。这里的 Redis 仅指用户管理的目标 Redis 资源。用户、组织、项目、授权和长任务通过统一异步数据库适配层保存在 SQLite 或 MySQL/MariaDB；活动会话由进程内管理器执行，重启后恢复未完成任务为失败或重新排队。运行驱动由 `DATABASE_DRIVER` 和对应的 `DATABASE_*` 环境变量选择，不做双写。

### 12.2 主要依赖

| 模块 | 依赖 | 许可证 | 用途 |
| --- | --- | --- | --- |
| Web | Vue 3、TypeScript、Vite | MIT | 前端应用 |
| UI | Element Plus | MIT | 基础控件 |
| 终端 | `@xterm/xterm` 与 addons | MIT | 浏览器终端 |
| 调度 | Croner | MIT | 脚本与 SecureCRT Cron 定时同步 |
| SSH/SFTP | `ssh2` | MIT | SSH、跳板机和 SFTP |
| API | Fastify、`@fastify/websocket` | MIT | HTTP 和 WebSocket |
| MCP | `@modelcontextprotocol/sdk` | MIT | Streamable HTTP、STDIO、工具、Resource 与 Session |
| SQL 编辑 | Monaco Editor | MIT | SQL 编辑器 |
| 数据网格 | Tabulator | MIT | 可编辑表格 |
| MySQL/MariaDB | `mysql2` | MIT | 元数据库连接池，以及业务连接、查询、备份恢复和数据迁移 |
| Redis | `ioredis` | MIT | Standalone、ACL、TLS、二进制命令和服务端/桌面目标连接 |
| HTTP Tunnel | Navicat `ntunnel_mysql.php` 协议兼容层 | 自研，参考 MIT 客户端行为 | 请求级 MySQL/MariaDB Tunnel |
| SQLite | `better-sqlite3` | MIT | 默认元数据库、测试和 SQLite→MySQL 迁移源 |
| 节点监控 | Go、Telegraf `v1.39.2` | BSD-3-Clause / MIT | Linux 宿主机与服务采集、固定插件适配器 |
| 节点缓冲 | `modernc.org/sqlite` | BSD-3-Clause | 不依赖 CGO 的 SQLite/WAL 序列缓冲 |
| XML | `fast-xml-parser` | MIT | NCX/XML 解析 |
| XLSX | ExcelJS | MIT | XLSX 导入导出 |
| 密码 | `argon2` | MIT | 平台用户密码 |

### 12.3 UI 视觉方向

- 定位：专业、克制、高信息密度的“运维桌面”，不是营销型后台模板。
- 桌面采用固定应用壳、清晰的资源树、页签和可拖拽工作区。
- 应用壳使用固定在视口坐标系的 `22px × 22px` 连续工程网格；侧栏、粘性顶部栏和内容区保持同一网格原点，浅色、深色与明亮主题均可见。
- 卡片、筛选条和工作台使用实体表面覆盖背景网格；页面标题只保留必要的模块标识和主标题，不堆叠解释性副文案。
- 带图标按钮统一图标盒、基线、间距和行高，筛选胶囊使用紧凑字体层级。
- 浅色和深色主题继续使用深色控制台形成明确操作上下文；明亮主题为强环境光与屏幕反光场景提供完整浅色业务工作区，仅以最外侧深色用户菜单栏保留应用边界。
- 使用蓝绿色作为运行状态与连接强调色，琥珀色提示风险，避免泛紫渐变。
- 字体优先使用 Avenir Next / Noto Sans SC，终端使用 JetBrains Mono / SFMono。
- 关键动作有清晰 hover/focus/disabled/loading 状态。

## 13. 元数据库数据模型

SQLite 和 MySQL/MariaDB 使用同构业务表和外键关系。SQLite 保留 WAL 及启动时兼容迁移；MySQL/MariaDB 使用 InnoDB、`utf8mb4_unicode_ci` 和连接池。一次性 `database:migrate:mysql` 命令只接受空目标库，在事务内复制 SQLite 数据，并逐表核对行数与外键孤儿；迁移完成后运行时只写所选驱动。

主要表：

- `admin_users`
- `sessions`
- `organizations`
- `organization_members`
- `organization_invitations`
- `organization_invitation_policies`
- `organization_member_invitations`
- `projects`
- `project_members`
- `resource_grants`
- `environment_groups`
- `environments`
- `knowledge_nodes`
- `knowledge_node_environments`
- `knowledge_node_grants`
- `knowledge_assets`
- `web_entries`
- `web_credentials`
- `web_account_views`
- `connection_sources`
- `connection_source_runs`
- `source_folder_mappings`
- `ssh_keys`
- `ssh_connections`
- `database_connections`
- `redis_connections`
- `redis_connection_environments`
- `desktop_redis_credential_requests`
- `connection_import_batches`
- `connection_import_items`
- `ssh_terminal_recordings`
- `database_query_history`
- `database_query_favorites`
- `database_object_favorites`
- `connection_inspection_results`
- `environment_logs`
- `services`
- `service_deployments`
- `service_log_links`
- `service_script_actions`
- `monitor_hosts`
- `monitor_samples`
- `monitor_sequence_gaps`
- `monitor_install_tasks`
- `monitor_alert_settings`
- `monitor_alert_states`
- `monitor_alerts`
- `monitor_alert_user_states`
- `database_tasks`
- `audit_events`
- `settings`

约束：

- `admin_users` 是为兼容现有 SQLite 保留的表名，实际保存全部平台用户；`is_platform_admin` 与组织角色相互独立。
- 会话保存当前 `workspace_type/workspace_id`；切换到组织前必须验证当前用户仍是有效组织成员。
- 组织至少保留一名有效管理员；项目成员必须先是同组织成员。
- `organization_invitations` 保存邀请令牌摘要、邀请人和有效期；`organization_invitation_policies` 保存主密钥加密令牌、名额、已使用数和撤销状态，旧的一次性邀请没有策略行时按 1 个名额兼容处理。
- 接受邀请时在同一事务内原子占用一个名额、写入组织成员和 `organization_member_invitations` 邀请人关联；并发争抢最后一个名额时只有一人成功，成员移除不会返还已经消耗的邀请名额。
- 工作空间根资源使用 `workspace_type/workspace_id` 隔离，环境组名称和连接组路径只要求在同一工作空间内唯一。
- SSH 密钥名称只要求在同一工作空间内唯一；被 SSH 连接引用时拒绝删除。个人资源复制到组织时按公钥指纹复用组织密钥，否则复制并重新加密私钥。
- 环境的 `group_id` 可为空。
- 知识库节点删除按父子关系级联，环境标签、节点授权和图片随节点删除；组织成员或项目组被移除时，其知识库编辑授权同步删除。删除环境只级联删除知识节点的环境标签，不删除节点、正文、图片或授权。
- SSH/DB/Redis 连接保留兼容的单值 `environment_id`，实际多环境关系由各自的 `*_connection_environments` 表维护；没有关联时进入待分配队列。
- 来源 ID + 原始路径在同一来源内唯一。
- 凭据表只保存加密列，不保存明文列。
- `web_account_views` 只保存用户、凭据和最后地址元数据；Cookie、缓存和 LocalStorage 位于按用户与凭据隔离的 Chrome Profile 目录。
- `desktop_devices` 只保存用户设备的 RSA 公钥、指纹和状态；私钥由 App 使用当前安装的本机随机密钥加密保护，不上传中心服务。设备注册挑战只保存短期哈希，一次性凭据请求 ID 在有效期内保持唯一。`desktop_operation_reports` 按操作 ID 保存设备签名报告的稳定载荷摘要，在审计保留期内拒绝不同载荷复用同一操作 ID，并允许相同载荷幂等重试。App 服务端转发 SSH 的 Agent 现场读取、只读诊断和取消同样使用最多 60 秒有效的 RSA-PSS-SHA256 设备签名；签名绑定用户、工作空间、SSH 会话、App 执行 Scope、动作、执行 ID 与命令，其中执行 ID 同时作为一次性操作 ID，中心拒绝同一授权再次执行。
- 服务名称在环境内唯一。部署节点以服务、SSH 连接、类型和外部标识唯一；删除服务级联删除部署节点与日志关联，删除 SSH 连接会保留部署节点但把连接置空，删除日志配置只删除关联。
- `monitor_hosts` 与 SSH 连接记录一一对应，保存 Agent 身份、协议版本、共享游标投影、最近快照、托管安装路径、架构与安装时间；样本与缺口仍按 `SSH 连接 + Agent + sequence_end` 幂等唯一，但同一工作空间与 Agent 只选择一条规范连接承载中心样本，其他重复连接共享读取。个人与组织空间不合并；删除 SSH 连接时级联清理该连接承载的中心监控数据，节点本地缓冲不受影响。
- `monitor_install_tasks` 保存安装目标、阶段、进度、脱敏日志、错误和结果；删除环境或 SSH 连接时级联删除任务。活动任务按 SSH 连接互斥，任务输出在写库和返回客户端前移除私钥块、认证头、密码、令牌、密钥字段及 URL 内嵌密码。
- `monitor_alert_settings` 以环境为主键保存各规则开关、阈值和排除磁盘；`monitor_alert_states` 保存连续异常/恢复计数、磁盘基线和已发事件引用；`monitor_alerts` 保存不可变的触发目标快照及 `active/recovered/event` 生命周期，其中 `event` 用于磁盘新增等一次性通知；`monitor_alert_user_states` 保存每个用户的异常通知、恢复通知和已读状态。删除环境级联删除全部告警数据，删除连接、服务或部署节点只清空事件中的可跳转外键，保留已经发生的告警记录。
- 删除环境时连接 `environment_id` 置空。
- SQL 历史、SQL/对象收藏、数据库任务和终端录像保存用户归属；审计事件保存操作者及操作发生的工作空间。

## 14. API 设计

API 前缀：`/api/v1`

### 14.0 产品与协议发现

- `GET /healthz`：返回运行状态、兼容保留的 `version`、`product=viron`、根 `package.json` 中的 `productVersion`、API 协议范围和当前服务端转发能力。
- `GET /api/v1/capabilities`：无需登录即可返回产品标识、产品版本、API 协议最小/最大值、`desktopLocal.web/ssh/sftp/logs/database/redis/inspection` 及对应服务端转发分项能力；不返回连接、凭据或内部地址。API 协议 v2 表示中心服务支持本机 Web、SSH/SFTP/日志、数据库、Redis 和连接巡检共用的设备密钥、一次性凭据信封和签名报告接口。
- `GET /api/v1/version`：无需登录返回服务端产品版本、当前 API 协议版本与兼容范围，以及统一安装包清单中 macOS `arm64/x64`、Windows `x86/x64/arm64` 的客户端版本、可用状态、固定同源下载 URL、真实文件名和大小。服务端与所有客户端的产品 SemVer 只来自根 `package.json`。
- `GET /api/v1/desktop-installers/:platform/:architecture`：无需登录从统一安装包清单流式返回精确平台/架构包；精确架构不存在时回退到同平台 Universal 包。没有匹配项时返回 `DESKTOP_INSTALLER_NOT_AVAILABLE`，不回退到其他具体架构。
- `GET /api/v1/client-installers`：登录后返回与自动更新相同的 `DATA_DIR/installers` 发布清单。扫描只接受根层非空普通文件、macOS `.dmg` 或 Windows `.exe`、当前产品版本和可识别的 `arm64/x64/x86/universal` 架构；同一平台与架构存在多项时以文件更新时间和文件名稳定选出一项。不递归目录，不公开服务端绝对路径，响应禁止缓存。
- `GET /api/v1/client-installers/:fileName/download`：登录后按统一清单白名单流式下载对应文件；下载时重新校验文件仍然是当前发布项，不提供任意路径读取。Web 清单、手工下载、版本接口与登录前自动下载均在每次请求时重读同一目录，不维护独立安装包路径配置。
- 桌面 App 在登录前同时校验两个接口，并选择客户端与服务端协议范围交集中的最高版本；产品不符、证书失败或协议无交集时不允许登录。

### 14.1 认证

- `POST /auth/register`
- `POST /auth/login`
- `POST /auth/logout`
- `GET /auth/me`
- `PUT /auth/password`
- `PUT /auth/workspace`
- `/users`、`/users/:id/status`、`/users/:id/password`：平台管理员的账号创建、启停与密码重置。
- `/organizations`：当前用户的组织列表与组织创建；创建是账号级动作，不依赖当前个人或组织工作空间。
- `POST /organizations/:id/invitations`：组织管理员组合有效期与可加入人数生成邀请令牌。
- `GET /organizations/:id/invitations`、`DELETE /organizations/:id/invitations/:invitationId`：组织管理员查看链接创建者、到期和名额状态，重新复制可恢复链接或将其撤销。
- `GET /organization-invitations/:token`、`POST /organization-invitations/:token/accept`：登录用户查看邀请人与组织，并确认以普通成员身份加入。
- `/organizations/:id/members/:userId`：组织成员移除和管理员角色调整。
- `/organizations/:id/projects`、`/organizations/:id/projects/:projectId/members`：项目与项目成员维护。
- `/organizations/:id/grants`：面向用户或项目的资源授权和撤销；创建授权同时兼容单个 `resourceId` 与 1–500 个同类型 `resourceIds` 批量请求，批量校验、授权和审计在同一事务中完成。

### 14.2 环境

- `/environment-groups`：CRUD；`PUT /environment-groups/order` 提交当前工作空间完整的环境组 ID 顺序。
- `/environments`：CRUD、搜索、筛选、统计；`PUT /environments/order` 原子提交当前工作空间全部环境的顺序与分组。
- `/environments/:id/web-entries`：Web 页面 CRUD；`PUT /environments/:id/web-entries/order` 事务保存当前环境全部 Web 入口顺序。
- `POST /web-credentials/:id/view` 创建或复用普通 Web 的账号页面；`DELETE /web-credentials/:id/view` 只关闭当前用户、当前执行 Scope 的活动页面，不删除 Profile/Cookie，用于撤销尚未接管的页面预热。
- `/environments/:id/logs`、`/environment-logs/:id`：环境日志配置 CRUD。
- `GET /knowledge`、`POST /knowledge/nodes`：读取当前工作空间的完整知识树以及创建未分配环境的文件夹或文档。
- `GET /environments/:id/knowledge`、`POST /environments/:id/knowledge/nodes`：读取按有效环境标签筛选的知识树，以及创建自动携带当前环境标签的文件夹或文档。
- `PATCH /knowledge-nodes/:id/environments`：在节点编辑权和目标环境访问权校验后增删直接环境标签。
- `GET /environments/:id/knowledge/association-candidates`、`POST /environments/:id/knowledge/associations`：读取当前用户可编辑且尚未在环境中有效展示的文档，并批量关联当前环境。
- `GET /knowledge-documents/:id`、`PUT /knowledge-documents/:id/content`：读取 Markdown 正文/Base64 图片并以版本号保护保存。
- `PUT /knowledge-nodes/:id`、`DELETE /knowledge-nodes/:id`：重命名、移动和递归删除知识节点。
- `POST /knowledge-documents/:id/assets`、`DELETE /knowledge-assets/:id`：上传或删除文档 Base64 图片，单图最大 30 MB。
- `/knowledge-nodes/:id/grants`、`DELETE /knowledge-grants/:id`：组织管理员向成员或项目组分配、查看和撤销节点编辑权。
- `POST /knowledge/import`、`POST /environments/:id/knowledge/import`、`GET /knowledge-nodes/:id/export`、`GET /knowledge/export`、`GET /environments/:id/knowledge/export`：全局或环境内 Markdown/ZIP 导入，以及文档、文件夹、工作空间知识库或环境筛选结果导出。
- `POST /environment-logs/:id/stream`、`DELETE /ssh-log-streams/:id`：启动和停止受控日志流；启动请求可提交 1–5,000 的 `initialLines` 作为行数上限，省略时使用 200。
- `GET /environments/:id/maintenance`：返回服务、部署节点、日志快捷关联，以及当前用户有权访问的环境 SSH 主机最近快照与按来源候选；响应通过 `canConfigure` 与 `canOperate` 区分配置和操作权限。
- `POST /environments/:id/services`、`PUT|DELETE /services/:id`：创建、更新、停用或删除服务，仅工作空间所有者和组织管理员可用。
- `PUT /environments/:id/services/order`、`PUT /environments/:id/maintenance-hosts/order`：分别事务保存当前环境完整服务 ID 顺序和完整 SSH 连接 ID 顺序；仅工作空间所有者和组织管理员可用，缺失、重复或越权 ID 会被拒绝。
- `POST /services/:id/deployments`、`PUT|DELETE /service-deployments/:id`：纳管、修复或移除部署节点；连接必须仍属于服务所在环境。
- `PUT /services/:id/logs`：以当前环境日志 ID 集合替换服务日志关联。
- `POST /services/:id/script-actions`、`PUT|DELETE /service-script-actions/:id`：创建、更新或删除服务级/节点级功能按钮；配置仅限工作空间所有者和组织管理员，脚本正文不写入审计。
- `POST /service-script-actions/:id/execute`：经对应部署节点的服务端 SSH 连接把固定脚本正文写入 `/bin/sh -s` 标准输入；服务级按钮作用于全部部署节点，节点级按钮只作用于绑定节点，并返回逐节点退出码、耗时和受限输出。
- `POST /environments/:id/monitor-hosts/:connectionId/install/preflight`：经服务端 SSH 返回目标系统、架构、systemd、权限、路径状态和同版本安装包可用性，不修改目标主机。
- `POST /environments/:id/monitor-hosts/:connectionId/install`：再次预检后经 SFTP 安装或更新监控服务，自动重新登录并拉取首批指标；目录冲突、旧手工安装和不满足权限的目标被拒绝并审计。
- `POST /environments/:id/monitor-hosts/:connectionId/install-tasks`、`GET /environments/:id/monitor-hosts/:connectionId/install-task`：启动后台安装任务并读取该 SSH 连接最近任务的真实阶段、进度、耗时、脱敏日志和结果；活动任务关闭页面后继续执行，同一连接拒绝并发安装。
- `POST /environments/:id/monitor-hosts/:connectionId/refresh`：通过服务端 SSH 立即执行一次采集并拉取；返回未安装、协议错误或 SSH 错误的明确状态，不向目标节点开放网络入口。
- `GET /environments/:id/monitor-hosts/:connectionId/history?range=1h|6h|24h|7d|30d`：读取当前环境和工作空间内按 Agent、序列去重的监控历史，返回约 480 个绘图点、断档、宿主机完整资源指标、受限 Top 进程摘要、趋势统计、诊断区间及当前环境已纳管部署指标。
- `GET|PUT /environments/:id/monitor-alert-settings`：读取或由工作空间所有者/组织管理员更新当前环境宿主机离线、指标、磁盘挂载变化和部署状态告警规则；更新时结束旧活动状态并以最近有效快照静默建立磁盘基线。旧客户端未提交 `hostOfflineEnabled` 时保留既有值，新环境不会由旧客户端静默启用该规则。
- `GET /monitor-alerts`、`POST /monitor-alerts/:id/notified`、`POST /monitor-alerts/:id/read`、`POST /monitor-alerts/read-all`：按当前工作空间和环境授权读取最近活动告警、恢复记录与一次性事件、未读数及待提醒阶段，并持久化当前用户的异常/事件/恢复通知与已读状态。
- `PUT /environments/:id/monitor-hosts/:connectionId/kubernetes-contexts`：校验 `sourceId + context` 必须来自该节点最近发现清单，经 SSH 保存选中范围并立即采集；请求和中心数据库都不接收 kubeconfig 凭据。
- `POST /environments/:id/monitor-hosts/:connectionId/clear`：显式清理目标 SSH 主机上的节点样本和缺口；不删除中心已经入库的数据，并记录成功或失败审计。
- `POST /service-deployments/:id/actions`：执行固定的 `start|stop|restart` 原生动作并刷新监控状态；Kubernetes、普通进程、停用服务和不可用 SSH 连接被拒绝。
- `/web-entries/:id/credentials`：多账号 CRUD、显示/复制审计；`PUT /web-entries/:id/credentials/order` 事务保存当前入口全部账号顺序。
- `POST /web-credentials/:id/view`：启动或恢复账号页面，并返回一次性 WebSocket 票据。
- `POST /web-credentials/:id/view/reset`：清除当前用户的该账号浏览器数据并重新登录。
- `POST /web-credentials/:id/view/upload`、`GET /web-view-downloads/:id`：页面文件上传与下载。
- WebSocket `/ws/web-account-view?ticket=...`：页面画面、状态和输入事件。
- `GET /desktop/devices/:deviceId`、`POST /desktop/devices/registration-challenges`、`POST /desktop/devices/registration-challenges/:challengeId/complete`：查询本用户设备状态，并通过 RSA-OAEP-SHA256 加密挑战完成 3072 位 RSA 私钥持有证明。
- `POST /desktop/web-credentials/:credentialId/envelope`：重新校验当前 Session、工作空间、资源权限和设备状态，返回绑定设备、请求、用户、工作空间、Endpoint、目标 Origin、凭据版本及 60 秒有效期的 RSA-OAEP-SHA256 + AES-256-GCM 一次性凭据信封。

### 14.3 连接与导入

- `/ssh-keys`：当前工作空间管理员列出密钥；`/import` 导入外部私钥，`/generate` 生成密钥对，`/:id` 重命名或删除，`/:id/export?part=public|private` 导出。普通组织成员统一返回 403，列表不返回私钥或口令。
- `/ssh-connections`：CRUD、测试连接、分配环境；托管密钥认证使用 `sshKeyId`，创建请求可带 `copyFromId`，服务端复制并重新加密源 SSH 凭据。
- `/database-connections`：CRUD、测试连接、分配环境；创建请求可带 `copyFromId`，服务端复制并重新加密源数据库与 HTTP Tunnel 凭据。
- `/redis-connections`：Redis CRUD、独立复制、环境关联、连接组选项和加密凭据；创建请求可带 `copyFromId`。
- `/connection-sources`：SecureCRT/脚本同步源 CRUD、立即同步、Cron 配置与目录映射。
- `/connection-sources/:id/runs`、`/connection-source-runs/:id`：读取脚本同步的脱敏执行报告与资源处理明细。
- `GET /connection-copy/catalog`：在组织管理员上下文中返回当前管理员可复制的个人资源目录，不返回任何明文凭据。
- `POST /connection-copy/preview`：展开跳板机、SSH Tunnel 和日志依赖，返回凭据数量及当前组织冲突候选。
- `POST /connection-copy`：校验复用目标与授权对象，在单一事务中复制环境组、环境、Web、SSH、数据库、日志、关系和授权。
- `/connection-imports/preview`、`/:id/confirm`：SecureCRT/Navicat 上传、预览和冲突确认。
- `/connections`、`/connections/assign`：资源池查询与批量环境分配。
- `POST /connections/inspect`：批量检查 SSH/数据库/Redis 连接并保存最近一次结果。

### 14.4 SSH/SFTP

- `POST /desktop/ssh-connections/:id/envelope`：App 提交已注册设备 ID、一次性请求 ID 和当前 Endpoint；服务端复核 Session、工作空间、主连接及单级跳板机后，返回绑定目标、连接版本且 60 秒有效的 RSA/AES-GCM 凭据信封。Renderer 不能直接调用该路径。
- `GET /api/v1/auth/execution-runtime`、`POST /api/v1/auth/execution-runtime/close`：App 主进程携带当前 Endpoint 的随机 `X-Viron-Execution-Scope`，查询或关闭当前用户在该 App 执行实例内的服务端 Web、SSH、SFTP、日志和数据库活动资源。缺失或无效实例标识时拒绝请求；普通 Web、其他 App 或其他 Endpoint 的资源不受影响。
- `POST /ssh-sessions`：创建会话并返回一次性 WebSocket 票据。
- `POST /ssh-sessions/:id/ticket`：生成重新附着票据。
- `DELETE /ssh-sessions/:id`：关闭会话。
- `/ssh-connections/:id/sftp/*`：列表、mkdir、rename、chmod、删除、上传、下载。
- `POST /sftp-transfers/preview`：检查来源类型、递归文件数/字节数、目标路径和同名冲突。
- `GET|POST /sftp-transfers`：列出当前工作空间任务或创建后台主机间传输。
- `DELETE /sftp-transfers/:id`、`POST /sftp-transfers/:id/retry`：取消和重试传输任务。
- WebSocket `/ws/ssh?ticket=...`。
- WebSocket `/ws/ssh-logs?ticket=...`：环境日志流。

App 的 SSH 会话、SFTP 操作、跨主机任务和本机录像不复用上述服务端执行 API；Renderer 通过 preload 白名单提交会话 ID、票据、终端输入/尺寸、远端路径和文件分片，由主进程在本机执行。退出登录、工作空间/Endpoint 切换及连接修改或删除会关闭受影响运行时。

### 14.5 数据库

- `POST /database-connections/:id/test`
- `GET /database-connections/:id/schemas`
- `GET /database-connections/:id/objects`
- `GET /database-connections/:id/ddl`
- `POST /database-connections/:id/queries`
- `GET|DELETE /database-queries/:queryId`
- `GET /database-connections/:id/table-data`
- `POST /database-connections/:id/table-data/changes`
- `/database-connections/:id/backup|restore|transfer`：创建备份、恢复或一次性数据传输后台任务。
- `POST /database-connections/:id/sync-preview`：连接源和目标 MySQL/MariaDB 数据库，返回数据同步表兼容性或结构同步对象差异、风险和 SQL 预览，不修改目标数据库。
- `POST /database-connections/:id/sync`：按比较结果中选中的对象创建数据同步或结构同步后台任务；服务端重新计算差异，不接受客户端提交任意 SQL。
- `/database-tasks`：进度、日志、下载和取消。
- `/database-query-favorites`：按用户、连接和数据库保存、更新、列出和删除 SQL 收藏。
- `/database-object-favorites`：数据库和数据表收藏列表、创建和删除。

### 14.6 Redis

- `POST /redis-connections/:id/test`：执行真实连接、认证、`PING` 与 Server INFO，返回延迟、版本和运行模式。
- `GET /redis-connections/:id/info`：读取当前节点结构化 INFO。
- `POST /redis-connections/:id/scan`：受限 `SCAN`，返回二进制键、类型、TTL 和不透明游标。
- `POST /redis-connections/:id/command`：执行命令策略明确允许的单条命令，参数可使用 UTF-8 字符串或 Base64 二进制对象，返回结构化二进制响应。
- `POST /desktop/redis-connections/:id/envelope`：向已注册桌面设备发放 Redis、TLS 与可选 SSH Tunnel 的一次性凭据信封，Renderer 禁止直接访问。
- `POST /desktop/redis-executions`：接收主进程签名的 Redis 本机执行报告并写入中心审计。

### 14.7 审计与设置

- `GET /audit-events`：按保留期分页查询操作事件，支持 `actorUserId`、`q`、`page` 和 `pageSize`。
- `GET /audit-actors`：返回当前工作空间保留期内可用于审计过滤的操作者。
- `GET /ssh-recordings`、`GET /database-query-history`：沿用相同的操作者、关键字和分页参数。
- `GET /settings`
- `PUT /settings`
- `POST /platform-exports`
- `POST /platform-restore`

### 14.8 MCP

- `GET|POST|DELETE /mcp`：个人 API Key 认证的 Stateful Streamable HTTP 入口；初始化后 Session 绑定用户、Key 和随机执行 Scope，单请求与 Broker 消息上限为 16 MiB。工具文本最多 512 KiB，结构化结果最多 12 MiB；超限时返回截断标记和预览，要求 Agent 缩小范围或分批读取。
- `PATCH /api/v1/api-keys/:id/mcp-approval-mode`：个人 Key 所有者更新远程 MCP 审批模式；平台 Key、已撤销 Key、非所有者或非个人 Key 被拒绝。创建个人 Key 可以同时提交初始审批模式，轮换 Key 时保留原模式。
- `GET /api/v1/mcp/business-operations`：保留为服务端内部兼容目录接口；MCP 客户端通过固定网关工具读取领域大纲、搜索操作摘要，并只按单个 operation 获取完整 JSON Schema。
- `POST /api/v1/mcp/operations`、`GET|DELETE /api/v1/mcp/operations/:id`：创建、读取和取消凭据安全输入或风险确认 Operation。需要人工确认的 confirmation Operation 初始状态为 `awaiting_purpose`，且此时公开响应不返回审批页地址。
- `POST /api/v1/mcp/operations/:id/purpose`：仅为 `awaiting_purpose` Operation 提交 8–80 字 Agent 执行意图，成功后转为 `pending` 并返回审批页地址；重复提交、凭据 Operation 或其他状态被拒绝。
- `GET /mcp/operations/:id`、`POST /mcp/operations/:id/submit|cancel`：同用户、同工作空间的登录 Session 安全页面；API Key 请求被拒绝。连接文件导入使用真实 multipart，单文件上限 8 MiB。
- `POST /api/v1/mcp/operations/:id/desktop-result`：App 使用一次性租约回传本机确认操作结果；租约不进入公开 Operation 响应，完成后立即失效。
- `/api/v1/mcp/ssh-connections/:id/command`、`/api/v1/mcp/environment-logs/:id/snapshot`、`/api/v1/mcp/web-credentials/:id/snapshot|action|control`：MCP 专用的受控执行端点。其余工具解析为既有业务 API，并统一经过 MCP 路径禁止策略。
- 桌面 App 启动 Broker 时生成 `viron-mcp.json` 和 Unix Socket/Named Pipe 实例。macOS 包内 `/Contents/MacOS/viron-mcp` 使用按目标架构编译并签名的原生 Mach-O 启动器，Windows 包根目录使用 `viron-mcp.cmd`；两者都以 Electron Node 模式启动 `dist/desktop/mcp-stdio.js`，只负责 MCP 协议与 Broker 转发。

## 15. SSH WebSocket 协议

客户端 JSON 控制消息：

- `input`：终端输入。
- `resize`：列数和行数。
- `ping`：保活。

客户端二进制消息：

- ZMODEM 等需要保留原始字节的终端输入，直接写入 SSH Shell。

服务端 JSON 控制消息：

- `ready`：PTY 和连接信息。
- `error`：可展示错误码和脱敏信息。
- `closed`：服务端会话关闭及原因。
- `pong`：保活响应。

服务端终端输出使用二进制 WebSocket 帧，普通文本由 xterm.js 直接消费，ZMODEM 协议字节交给协议处理器。服务端为每个会话保存有限字节缓冲，重附着时先发送缓冲内容，再转入实时流。

## 16. Docker 与交付

### 16.1 Compose

Compose 提供两个互斥的主服务入口，并为两者配置同一隔离 Runner：

- `docker-compose.lite.yml` 使用 `viron-server-lite:<version>`。镜像包含完整控制面 API、桌面客户端设备与凭据信封接口，以及 SSH/SFTP/日志/数据库/Redis 服务端转发；不复制浏览器客户端静态资源、不安装 Chromium，并声明 `clientAccess.web=false`、`serverForwarding.web=false`。
- `docker-compose.full.yml` 使用 `viron-server-full:<version>`。镜像在同一 Node 服务和 Origin 下提供静态页面、API、WebSocket 与全部服务端转发能力，安装 Chromium 并声明 `clientAccess.web=true`、`serverForwarding.web=true`。Chromium 只在用户双击页面空白处或点击“访问页面”后按需启动。
- 两个 Compose 文件都启动 `viron` 与 `script-runner`。`script-runner` 不发布端口、不挂载 `/data`，只挂载 `DATA_DIR/script-runner` 中的 Unix Socket 目录；它使用只读根文件系统、独立临时文件系统、能力白名单和 CPU/内存/PID 限额。主服务仍是唯一对外 HTTP/WebSocket 入口，不引入 Nginx。
- `HOST` 表示宿主机端口绑定地址；容器内部固定监听 `0.0.0.0`。`PORT` 同时用于宿主机发布和容器监听。`DATA_DIR` 表示宿主机路径，Compose 将其挂载到容器 `/data`。
- `DATABASE_HOST=127.0.0.1`、`localhost` 或 `::1` 在镜像内映射为 `host.docker.internal`，Linux Compose 通过 `host-gateway` 提供该名称；其他地址原样使用。
- 对外健康检查固定为同一 Viron Endpoint 下的 `GET /healthz`。

### 16.2 镜像

- 多阶段 Dockerfile：统一构建客户端与服务端；Node 构建与运行阶段共用 slim 系列基础镜像，服务构建跳过不会进入镜像的 Electron 二进制下载；监控安装包固定在 BuildKit 原生构建平台交叉生成 Linux `amd64/arm64` 产物，避免构建非宿主架构镜像时模拟执行 Go 工具链。Node、Go 和 APT 使用 BuildKit cache mount。
- Lite 只复制服务端与共享产物，不包含浏览器客户端静态资源或 Chromium。
- Full 从独立于应用源码的 `full-runtime` 阶段取得 Debian Chromium 和必要字体，再复制当前服务端与浏览器客户端产物；源码变化不会使重型系统依赖层失效，不使用 Nginx。
- 两种镜像都以 UID/GID 10001 的非 root `viron` 用户运行，SSH/SFTP 与数据库操作由 Node 库完成；监控安装包内置在 `/app/monitor`，源码运行可用 `VIRON_MONITOR_PACKAGE_DIR` 覆盖默认的 `dist/monitor`。
- Dockerfile 的 `script-runner` 目标只包含 Node、`/bin/sh`、curl、jq、OpenSSH、rsync 和 sshpass。Runner 主进程保留创建低权限子进程所需的最小能力，实际脚本以 UID/GID 10002、清空后的环境变量和独立临时目录执行。
- Compose 和全量发布脚本把每个目标的 BuildKit `mode=max` 缓存持久化到默认忽略的 `.tmp/docker-build-cache/`；缓存缺失时自动重建，Compose 的 `build --no-cache` 与发布脚本的 `--refresh-docker-cache` 用于显式刷新依赖层。缓存根目录、基础镜像代理和 APT 镜像源均可通过 `VIRON_*` 构建环境变量覆盖。
- 支持 `linux/amd64` 和 `linux/arm64`。
- 交付 `linux/amd64`、`linux/arm64` 两个可离线 `docker load` 的压缩包；每个压缩包都同时包含同版本的 Lite、Full 与 Script Runner 三个镜像。

### 16.3 macOS App

当前 Web/macOS 能力矩阵如下。这里的“对齐”指用户可见能力、权限判断和操作语义一致；目标流量允许按客户端可信边界采用不同执行位置。普通 Web 固定通过中心服务执行，macOS App 默认由主进程从当前 Mac 本机执行，也可在设置中按设备与 Endpoint 切换为服务端转发；失败不静默更换执行位置。

| 能力域 | 普通 Web | macOS App | 对齐边界与证据 |
| --- | --- | --- | --- |
| Endpoint、登录、个人/组织工作空间 | 当前站点 Session | 本地静态登录页、Endpoint 验证、隔离 Session Cookie | 共用中心认证、角色和资源授权；App 额外校验产品与 API 协议 |
| 环境、连接、同步、导入、复制、组织、用户、设置 | 中心 API | 受控主进程 API 代理 | 共用相同页面、路由、请求体和服务端权限；Renderer 不能越过已验证 Endpoint |
| 环境画中画 | 页面内实时缩略图叠层 | 主窗口透明原生子窗口 | 离开已连接环境后显示不可交互的 16:9 画面；悬浮展开、点击精确切换，App 原生层保持在内嵌 `WebContentsView` 上方 |
| Web 账号浏览 | 服务端 Chromium；页面检查器待交付 | 本机 `WebContentsView`，或 Full 中心的服务端 Chromium；本机页面支持检查元素 | 导航、刷新、多页面、上传、下载、重新登录和 Profile 清理一致；转发模式下中心不支持 Web 代理时明确保留本机执行；App 不提供账号明文显示/复制 |
| SSH 终端 | 服务端 `ssh2` + WebSocket | 本机 `ssh2` + preload IPC，或经主进程桥接服务端 WebSocket | 认证方式、单级跳板、登录脚本、多会话/分屏、输入、Resize、ZMODEM、历史和收藏一致 |
| SFTP | 服务端执行 | 本机主进程执行，或服务端执行 | 双栏浏览、CRUD、分片上传、系统下载、跨主机递归传输、预览、取消和重试一致 |
| 环境日志 | 服务端受控 `tail` | 本机主进程或服务端受控 `tail` | 配置 CRUD、1–10 文件、行数上限、高亮、清屏、停止和重连一致；Renderer 不能提交主机或任意路径 |
| MySQL/MariaDB 工作台 | 服务端数据库运行时 | 本机或服务端数据库运行时 | TCP、SSL/TLS、SSH/HTTP Tunnel、对象、DDL、查询/取消、表编辑、导入导出、备份恢复和迁移一致 |
| Redis 工作台 | 服务端 `ioredis` 运行时 | 本机主进程或服务端 `ioredis` 运行时 | TCP/TLS/SSH Tunnel、SCAN、六种核心类型、命令策略、INFO、Slow Log 与二进制模型一致；Renderer 不取得 Redis/TLS/SSH 凭据 |
| 连接巡检 | 服务端最多 5 并发 | 本机或服务端最多 5 并发 | 最多 500 条，执行 SSH 认证、数据库 `SELECT 1` 或 Redis `PING`；本机结果经设备签名后原子更新中心状态和审计 |
| 服务维护 | 中心服务经 SSH/跳板链路扫描、拉取、评估告警和执行 | 与普通 Web 使用相同中心 API 和后台拉取器 | 服务、部署节点、当前指标、进程组成趋势、自动性能诊断、阈值与磁盘挂载事件、未读列表、日志入口、原生动作与自定义脚本按钮一致；App 额外使用 macOS/Windows 原生系统通知，不跟随本机直连模式，App 关闭不影响周期拉取 |
| 审计、SQL 历史、收藏、录像 | 中心持久化 | 中心数据与本机录像合并 | 数据浏览语义一致；本机数据库与巡检报告具备签名、时效、上下文绑定和操作 ID 幂等 |
| 上传、下载与本地文件 | 浏览器选择/下载 | 原生文件选择器或系统保存对话框 | 用户选择语义一致；Renderer 不获得任意本地路径能力 |

环境组服务汇总仍未交付。个人本机审计队列、Developer ID、公证及正式商店级分发仍是路线图中的后续产品能力，也不属于当前 Web 已交付功能的桌面缺口。

- macOS App 支持 Apple Silicon `arm64` 与 Intel `x64`，最低系统版本为 macOS 12.0。Intel 构建的兼容基线覆盖 macOS Monterey 12.7.6；打包时必须验证所有 Mach-O、动态库和原生 Node 模块包含目标架构，并确认 Electron 主程序与 Framework 的部署目标不高于该基线。安装包必须声明 `NSLocalNetworkUsageDescription`，说明本机 Web、SSH、日志和数据库能力需要访问局域网目标。
- Electron 安装包只加载随 App 打包的 `dist/desktop-renderer` 静态资源，不加载中心服务的前端页面。
- Renderer 开启 Context Isolation、Sandbox 和 Web Security，只能调用 preload 暴露的 Endpoint、API 和下载白名单接口；主进程只接受当前主窗口消息，并把请求目标限制到已验证 Endpoint 的 `/api/*` 与 `/healthz`。
- Windows 主窗口使用 36px Window Controls Overlay 保留原生最小化、最大化和关闭按钮；Renderer 只能通过隔离 preload 提交 `light`、`dark`、`bright` 或登录页外观枚举，主进程校验后把原生按钮区域同步为对应 Viron 表面色与高对比符号色。主题或登录路由变化即时更新，页面操作区始终从可拖动标题栏下方开始。macOS 继续使用系统交通灯按钮和独立安全区。
- Endpoint 必须是无凭据、无路径/查询/Fragment 的 HTTP(S) Origin；协议和主机由用户决定，App 只通过真实健康检查、产品标识和 API 协议协商判断是否可用。选择 HTTPS 时仍执行正常 TLS 证书校验。
- 主进程按规范化 Endpoint 的 SHA-256 派生持久 Electron Session 分区，Session Cookie 不跨 Endpoint；App 不引入 Token，也不保存登录明文密码。
- 连接模式按“当前设备 + Endpoint”保存在 App 用户数据目录，默认 `local`。切换为 `server` 后，普通 API 继续经主进程的隔离 Session 访问 Endpoint，SSH、日志和服务端 Web 画面所需 WebSocket 由主进程携带 Session Cookie 建立并通过受控 IPC 转发字符串或二进制消息。每个 Endpoint 生成稳定随机的执行实例 ID，服务端 SSH、SFTP、日志、数据库任务与 Web Profile 均绑定该实例；查询、列出和模式切换清理只作用于“当前用户 + 当前执行实例”，不影响普通 Web 或其他 App。切换前先查询活动资源；有活动资源时确认并关闭，任一关闭失败则保持原模式。Web 在服务端未声明代理能力时使用本机执行，其他分项未声明服务端转发能力时显示不可用。
- 中心数据页面继续通过受控 IPC 访问。协商 API 协议 v2 且中心声明 `desktopLocal.web=true` 时，Web 账号使用 App 主进程创建的本机 `WebContentsView`：选中账号时 Renderer 只展示待访问界面，用户双击页面空白处或点击“访问页面”后才提交凭据 ID 与边界并创建本机视图；Profile 按 Endpoint、Viron 用户和凭据隔离，Renderer 后续只提交页面切换和受限导航动作，不能调用设备凭据接口或取得明文密码；主进程完成设备注册、凭据信封解封与原始 Origin 主页面自动填充。同一账号的 HTTP(S) 弹窗在共享 Profile 的独立 `WebContentsView` 中打开，每个账号最多 8 个页面，可切换和关闭；弹窗不自动填充凭据，`file:`、`javascript:`、`data:` 等非网络页面继续拒绝。目标页面右键“检查元素”由主进程消费原生 `context-menu` 坐标，对当前页面打开分离的 DevTools 并定位元素，不增加 Renderer IPC；DevTools 能访问当前 Profile 中该页面可见的 DOM、网络与存储，关闭目标页面时一并销毁。网站上传使用 Chromium 原生文件选择器；下载由主进程绑定到原账号并交给系统保存对话框，完成、取消或失败状态回传界面。重新登录会关闭同账号页面、清空该 Profile 并重新取得一次性凭据信封；账号或入口更新刷新本机视图但保留 Profile，删除账号、入口或环境时清理对应 Profile。环境沉浸导航在 App 中由主进程创建独立透明子窗口并使用单独的最小权限 preload，只接收脱敏的环境、入口和账号标识及受限导航动作；子窗口仅覆盖菜单自身边界，打开时不隐藏、缩放或向 Renderer 复制本机 Web 页面，外部点击直接到达原 `WebContentsView` 后再收起菜单。同窗聚焦模式通过路由状态隐藏 App 导航区，不增加读取网页内容或凭据的 IPC。摄像头、麦克风、地理位置等高权限默认拒绝。
- App 的环境画中画沿用透明原生子窗口边界，但与沉浸导航独立：Renderer 提交当前用户和工作空间筛选后的环境名称、按最后访问工作区排序的连接元数据及内存中的降采样 JPEG；提交前显式复制边界、连接和缩略图对象，保证 Vue 响应式代理不会进入 Electron IPC。主进程校验状态尺寸、图片协议与上限、语言和动作类型后转发。原生层不接收连接凭据、目标地址或可执行页面，只显示不可交互图片；环境切换和按环境关闭全部连接仍回到主 Renderer 的既有路由、确认框、权限和活动连接 API。原生窗口不可聚焦并始终非激活显示，悬浮只请求展开，鼠标移出请求收起；整张卡片的非按钮区域同时承担短按切换和拖动手势。
- 协商 API 协议 v2 且中心同时声明 `desktopLocal.ssh=true`、`desktopLocal.sftp=true` 时，App 开放与普通 Web 共用的 SSH 工作台。主进程通过当前 Session 获取请求绑定、60 秒有效且包含主连接和可选单级跳板机的 SSH 凭据信封，支持密码、私钥/口令和 keyboard-interactive，直接从本机建立 PTY、执行登录脚本并维护最多 20 个会话、一次性附着票据和 512 KB 输出缓冲。Renderer 只通过 IPC 收发字符串或二进制终端流和 Resize，不接收凭据；ZMODEM 保持原始字节顺序。终端输出写入 App 用户数据目录的 asciinema cast 索引，异常退出时标记为 interrupted，并与中心录像合并展示。
- App SFTP 由主进程按操作重新取得有权访问的连接凭据；在本机执行模式下，双栏选择器还提供“本机”位置，由主进程访问当前电脑文件系统。远端 SSH 连接和本机位置均支持列表、mkdir、rename、delete、chmod、256 KB 分片上传和系统保存对话框下载。跨位置文件/目录传输在 App 主进程中转，支持 SSH↔SSH、SSH↔本机、本机↔SSH 的预览、覆盖/跳过、进度、取消和重试，最多并发 3 个任务。Renderer 不能提交目标主机、端口、凭据或任意本地保存路径；远端目标来自中心最新连接，保存路径来自系统对话框。Endpoint、用户、工作空间或连接配置变化会关闭受影响会话和任务；中心临时不可达时已建立会话与已启动任务继续运行，新连接必须重新授权。本机 SSH/SFTP 失败不回退到服务端执行。
- 协商 API 协议 v2 且中心声明 `desktopLocal.logs=true` 时，环境日志配置 CRUD 和查看界面复用普通 Web 组件，Renderer 只提交环境 ID、日志配置 ID 和 1–5,000 的行数上限。关键字过滤、上下文、高亮和下载当前屏幕结果均在 Renderer 当前缓冲内完成；下载到本地文件时由主进程弹出系统保存对话框。主进程重新从中心读取有权访问的最新日志配置并取得对应 SSH 一次性凭据信封，固定生成受控的 1–10 文件 `tail -n <lines> -F -- <quoted paths>`；页面不能提交主机、凭据或任意路径。App 最多同时保留 3 路本机日志流，支持登录脚本、stdout/stderr、UTF-8 分片、停止和重新连接；Endpoint、用户、工作空间、SSH 连接或日志配置变化会关闭受影响流。
- 协商 API 协议 v2 且中心声明 `desktopLocal.database=true` 时，App 开放普通 Web 的完整数据库工作台，但目标连接由主进程在当前 Mac 建立。中心按数据库连接 ID 发放请求绑定且 60 秒有效的一次性信封，载荷包含数据库密码、SSL 客户端材料、HTTP Tunnel 凭据，以及数据库 SSH Tunnel 所需的主 SSH 连接和可选单级跳板机；受保护声明绑定数据库主机、端口、连接模式、HTTP Tunnel Origin、所有连接版本、Endpoint、用户和工作空间。Renderer 只提交连接 ID、SQL、对象参数、表数据变更或用户选择的导入文件，不取得数据库或 Tunnel 明文凭据。主进程支持 TCP、SSL/TLS、SSH Tunnel、Navicat HTTP Tunnel、对象树、DDL、最多 10 个并发查询、查询取消、单结果集 10,000 行限制、表数据编辑、最多 100,000 行 CSV/XLSX/SQL 导入导出、SQL 备份恢复和最多 3 个本机后台任务。查询历史和操作审计通过设备私钥签名报告按操作 ID 幂等回传中心；任务索引和备份保存在 App 用户数据目录，重启后未完成任务标记为中断。Endpoint、用户、工作空间或数据库配置变化会取消受影响查询和任务，本机失败不回退到服务端。macOS 主进程连接 RFC 1918、IPv4 链路本地或 IPv6 私网地址并收到 `EHOSTUNREACH` 时，返回独立错误码；工作台说明可能的本地网络授权异常、保留原始技术信息，并通过受控 IPC 打开本地网络系统设置。该判断不覆盖公网地址或其他操作系统。
- 协商 API 协议 v2 且中心声明 `desktopLocal.redis=true` 时，App 复用普通 Web 的 Redis 工作台，主进程按操作取得请求绑定且 60 秒有效的 Redis 凭据信封，在当前 Mac 建立 TCP/TLS/SSH Tunnel 短连接。Renderer 只提交连接 ID、逻辑库、扫描选项或结构化命令参数；主进程与服务端复用命令策略、二进制模型、资源限制和错误语义，本机执行报告使用设备签名回传审计，失败不回退到服务端。
- 协商 API 协议 v2 且中心声明 `desktopLocal.inspection=true` 时，连接巡检复用普通 Web 的筛选、选择、汇总与最近状态界面，但 `/api/v1/connections/inspect` 由主进程拦截并在当前 Mac 执行。Renderer 最多提交 500 个连接类型与 ID；主进程重新读取当前工作空间连接元数据，按项取得 SSH、数据库或 Redis 一次性凭据信封，以最多 5 并发完成 SSH 认证、数据库 `SELECT 1` 或 Redis `PING`。结果只包含连接 ID、名称、主机、端口、状态、耗时和脱敏错误，不包含凭据、连接选项或 Tunnel 材料。本机执行失败不回退到服务端；整批结果使用设备 RSA-PSS-SHA256 私钥签名，中心在同一数据库事务内校验 Session、管理员权限、用户/工作空间、设备状态、签名、60 秒时效和操作 ID，再更新 `connection_inspection_results` 与审计，确保业务写入与防重放登记原子提交。
- macOS 在 `app.ready` 前启用 Chromium 无钥匙串后端，运行时不创建或读取 `Viron Safe Storage`。设备私钥和 Viron Agent API Key 由 App 数据目录中的 `.local-secret-key` 提供安装级 256 位随机密钥，使用 AES-256-GCM 加密并通过用途与 Endpoint/用户派生上下文绑定；密钥文件以当前用户只读写权限保存。旧版钥匙串密文不会被重新读取：设备身份按 Endpoint/用户自动重建，Agent API Key 标记为未保存并等待用户重新填写。Windows 继续使用当前用户的操作系统安全存储。设备私钥和解封后的 Web/SSH/数据库/Redis 凭据只在主进程使用，已连接对象不保留凭据字段引用。
- App 主进程随登录实例启动受控 MCP Broker，并在退出、登出、Endpoint/工作空间或执行模式切换时取消未完成 Operation、关闭 Broker 或清理当前执行租约。安全 Operation 窗口固定使用当前 Endpoint 的登录 Session 分区、Context Isolation、Sandbox、Web Security 和权限全拒绝策略，只允许同 Origin 的精确 Operation 页面及提交/取消路径。窗口为可关闭的非模态窗口，不阻塞主界面；主进程轮询 Operation 状态并在完成、失败、取消或过期时销毁窗口，用户主动关闭仍在等待的窗口时取消 Operation。自动审批返回一次性执行租约时，App 不创建窗口而是立即执行并回传结果。
- App 每次启动恢复最近 Endpoint 或手工切换 Endpoint 后，由主进程通过该 Endpoint 的隔离 Session 查询版本。只有中心产品版本高于 App、当前平台/架构安装包存在且下载 URL 仍在 `/api/` 白名单内时才显示系统原生更新提示；相同版本或中心版本更低时不降级。确认更新后，macOS 与 Windows 共用独立的受限更新窗口，使用内置 Viron 标记显示下载字节、进度与校验/安装状态；不得为显示图标调用操作系统文件图标加载器。下载阶段允许由按钮或关闭窗口触发 `AbortSignal` 取消，随后清理临时文件且不显示失败提示，进入校验和安装后窗口不可取消。下载写入 App 用户数据目录下的专用更新临时目录，按服务端声明和 HTTP `Content-Length` 校验完整性，并同步更新窗口与系统级进度。更新窗口只能通过独立 preload 发送取消事件；普通 Renderer 与更新窗口都不接收安装包字节、本地路径或进程执行能力。服务端配置的安装包文件名必须包含与当前产品相同的 `Viron-<version>-` 前缀，旧版本或命名不匹配时拒绝启动，不能以新版本元数据分发旧包。
- macOS 更新先在主进程验证 DMG，再启动独立辅助进程：辅助进程先挂载只读安装盘并深度校验签名与版本，若目标目录不可写则在旧 App 仍位于前台时弹出系统管理员授权。授权或可写目录确认后，主进程才退出。辅助进程有界等待旧 PID 以及目标 `.app` 内全部 Viron 进程结束后，对**当前正在运行的** `.app` 做 staging/backup 替换；从 `/Volumes/` 只读安装盘启动时回退到 `/Applications/Viron.app`。最终签名或版本校验失败时恢复旧 App。Windows 更新先让 PowerShell 辅助进程写入就绪状态，主进程确认后再退出；辅助进程最多等待旧 PID 30 秒并清理仍占用同一可执行文件的进程，再以 NSIS `/S /D=<当前安装目录>` 覆盖安装，按 ProductVersion 前三段验收。两端成功时清理临时安装文件并启动新 App；失败时写入 `update-install.log`、展示失败原因并重新打开可用的旧版本。App 使用单实例锁；启动时若检测到未完成的安装辅助进程，则提示等待而不清理安装目录。当前 macOS 包为稳定本地自签名身份，Windows 包未签名，因此操作系统仍可能显示相应信任或 SmartScreen 提示。
- `./scripts/package-macos.sh` 先按锁文件同步完整依赖，再为当前 CPU 架构生成 `.app` 与 `.dmg`；可以通过 `--arch=arm64`、`--arch=x64` 或 `--all` 选择产物。对应的 `npm run package:macos`、`package:macos:arm64`、`package:macos:intel` 和 `package:macos:all` 复用同一入口。桌面 staging 在编译前清理 `dist/desktop` 与 `dist/shared`，避免已删除源码的旧编译文件进入 App；随后只收集主进程实际使用的运行依赖，使离线 App 不依赖构建机的 `node_modules`。DMG 组装保留 App 内 Framework 的相对符号链接，挂载验收会拒绝指向构建机或 App 包外的链接，并对安装原件重新执行严格深度签名校验。所有产物复用构建 Mac 专用钥匙串中的稳定自签名 Code Signing 身份并使用 Hardened Runtime 签名。该产物用于开发和受控内部分发，不等同于 Developer ID 签名与 Apple 公证。
- `npm run package:windows:x86`、`package:windows:x64`、`package:windows:arm64` 与 `package:windows:all` 在 macOS 构建机上生成对应架构的 NSIS `.exe` 安装包。Windows 包与 macOS 包复用同一份 Renderer、主进程、preload 和纯 JavaScript 运行依赖；staging 不复制 SSH 的 `cpu-features`、`nan` 和本机 crypto build 等可选宿主机原生模块。构建必须验证包内 `Viron.exe` 分别为 PE32 Intel 80386、PE32+ x86-64 或 PE32+ Aarch64，验证外层文件为 NSIS 安装器，并检查 `app.asar` 的桌面入口及 SSH、数据库、导入导出依赖完整。NSIS 启动壳固定为 PE32 x86，不作为包内 App 架构证据。macOS 构建机不宣称完成 Windows 运行验收；当前安装包未签名，正式支持前必须在对应架构的真实 Windows 上完成安装、启动、安全存储和完整工作台回归。

### 16.4 持久化目录

```text
/data/envman.db                    # SQLite 模式及迁移回退副本
/data/master-key                   # 实例托管主密钥，权限 0600
/data/installers/                  # Web 手工下载与 App 自动更新共用的客户端安装包
/data/recordings/
/data/web-profiles/
/data/web-downloads/
/data/backups/database/
/data/exports/
/data/uploads/
/data/restore-pending/
/data/migration-history/
```

平台迁移只由管理员手工触发，不定时执行。SQLite 直接生成一致性快照；MySQL/MariaDB 在读事务中导出同构 SQLite 快照，以支持同驱动或跨驱动导入。导入在重启阶段应用：SQLite 原子替换文件，MySQL/MariaDB 在关闭外键检查的单一事务中替换平台表；应用前均保留恢复前快照。迁移不能替代基础设施备份，`/data`、MySQL/MariaDB 数据库及其时间点恢复能力仍需独立保护。

## 17. 验收标准

### 17.1 基础与环境

- 首次 Compose 启动后初始平台管理员可以登录；普通用户可以自主注册并修改密码。
- 普通 Web 登录用户可以从侧栏进入客户端下载页；清单与 `DATA_DIR/installers` 中每个平台和架构的最新受支持安装包一致，点击卡片可取得原文件，旧版本下载地址不可用，桌面 App 不显示该入口。
- 不同用户的个人资源互不可见；平台管理员不会仅凭平台身份访问其他个人或组织资源。
- 用户可以在个人工作台和多个组织工作台间切换，列表、搜索、新建、WebSocket 和后台任务不混用其他空间数据。
- 组织普通成员只能看到直接授权与项目授权并集内的资源，不能查看明文凭据、修改组织资源或继续授权。
- 组织管理员可以按资源类型一次选择一个或多个资源授权给成员或项目组；选择器排除当前目标已直接获得或从祖先项目组继承的同一资源授权，批量请求任一资源无效或授权已存在时整批不写入。
- 组织管理员可以组合四种预设有效期与 1、3、5、10、不限或自定义名额生成邀请链接，并查看、复制和撤销已创建链接；受邀用户可以直接打开链接，或在 Web/macOS App 的“组织与用户”页面粘贴完整链接进入邀请确认；未登录访问会先进入登录或注册，随后返回邀请确认；过期、名额耗尽、已撤销和重复加入均不会新增成员，并发使用最后一个名额不会超额。
- 通过链接加入的成员在成员目录中显示邀请人；邀请名额与成员写入使用同一事务，失败或重复加入不会消耗名额。
- 环境组与环境授权动态覆盖以后新增的下级资源；撤销一条授权路径后仍存在其他路径时访问继续有效。
- 组织始终保留至少一名有效管理员；停用、移除成员和撤权会使相关会话及活动工作台立即失效。
- 可以创建有组和无组环境。
- 删除环境后只解除该环境的连接关联；没有其他环境关联的连接回到待分配资源池。
- 一个环境可以配置多个 Web、SSH、DB 和 Redis 资源。
- 一条 SSH、数据库或 Redis 连接可以同时关联多个环境，且能从每个关联环境的工作台访问。
- SSH 连接可以保存和展示多个自定义标签，并能按标签搜索。
- SSH、数据库和 Redis 连接可以基于现有配置创建独立副本，凭据不会返回浏览器，修改副本不影响源连接。
- 组织管理员可以批量复制自己的个人连接、环境和环境组；依赖、跨环境关系、复用冲突、项目/成员授权与失败回滚符合连接工具定义，复制前后的密文不同且明文等价。
- Web 密码可显示/复制，且产生审计。

### 17.2 导入与资源池

- 脚本同步可以在个人或组织空间自动写入全部已定义业务资源，并正确解析环境、密钥、跳板机、Tunnel、配置档与日志依赖。
- `ignore` 和 `overwrite` 策略行为稳定；脚本未返回的空间资源只进入报告，报告和审计不包含任何凭据或原始输出。
- Runner 无法读取 Viron 数据目录或服务环境，并能真实执行网络脚本、限制超时和输出大小。
- SecureCRT 同步源可以拉取真实会话树并生成导入预览。
- 同步更新不丢失已有环境关联。
- Navicat NCX/XML 或其 ZIP 包可以生成连接预览。
- 待分配连接可以批量关联到一个或多个环境。
- 目录映射对后续新连接生效。
- 跨来源疑似重复不会自动合并。
- 批量巡检可以筛选连接、排除目标，并在列表中保留最近一次结果。

### 17.3 SSH/SFTP

- 密码和私钥 SSH 均能建立真实终端。
- 至少四个终端可以同时运行并四分屏。
- 页面刷新后可以恢复未超时会话。
- Resize 正确改变远端 `stty size`。
- SFTP 上传下载内容校验一致。
- SFTP 作为 SSH 工作台终端内容区内的右侧双栏抽屉使用；桌面视口下左侧服务器列表和部分终端保持可见，抽屉可收起并重新打开。左右栏连接、路径、历史与选中项互不混用，面包屑可浏览和双击编辑，筛选与“操作”菜单可用。环境内主机选择器优先推荐当前环境连接但不排除其他有权访问的连接；App 本机执行模式额外提供“本机”位置，活动 SSH 会话连接在各自分组内优先展示。
- 文件和目录可以在两条有权访问的 SSH 连接之间递归复制；App 本机执行模式还可以在 SSH 连接和本机位置之间复制。普通 Web 由服务端中转，App 由本机主进程中转，传输前显示确定的来源、目标和同名冲突，覆盖/跳过结果符合选择。
- macOS App 使用本机 `ssh2` 覆盖密码、私钥/口令、keyboard-interactive、单级跳板机、登录脚本、字符串/二进制终端输入、PTY Resize 和本机 asciinema 录像；真实 Electron 测试通过 preload IPC 完成会话创建、附着、输入、关闭和录像检查。
- macOS App 的本机 Web 页面右键“检查元素”后，为当前页面打开分离的 Chromium DevTools 并定位到点击元素；切换账号或弹窗不会把调试目标错误绑定到其他页面，关闭目标页面后调试会话同时关闭，Renderer 始终不获得通用 DevTools IPC。
- macOS App 的 SFTP CRUD、分片上传、下载、SSH↔SSH 递归复制、SSH↔本机复制、冲突策略、取消和重试由本机运行时验证；Renderer 获得的会话与文件接口中不含 SSH 明文凭据。
- 主机间传输在切换工作区标签后继续运行并持续报告进度、速度、结果和错误；运行中任务可取消，失败或已取消任务可重试。
- 登录脚本只在启用且 Shell 建立后执行。
- 本地命令历史按用户和连接隔离，最多保留 50 条，连续重复命令不追加；输入建议优先匹配前缀，鼠标或方向键选择后可用 `Enter`/`Tab` 回填且不自动执行，开关按用户保存在当前浏览器。
- Web 端终端鼠标选择后自动复制，右键点击不得读取或粘贴系统剪贴板；复制不可用时给出原因，粘贴保留 `Ctrl/Cmd+V` 键盘路径。
- SSH、数据库和日志配置连接项的右键菜单只操作当前项目，与各自原有的可聚焦操作入口保持相同动作和可用状态。
- ZMODEM `rz`/`sz` 保持文件名、大小和二进制内容一致。
- 环境日志只能执行由服务端生成的固定参数多文件 `tail`，行数上限和文件数均受限，停止或断开后释放 SSH 通道。
- 环境日志使用启用登录脚本的 SSH 连接时，登录脚本和受控 `tail` 在同一 PTY Shell 中依次执行；未启用登录脚本时仍直接执行固定 `tail`。
- 环境日志语义高亮和关键字高亮都不执行日志中的 HTML 或脚本，关闭语义高亮后显示的原始文本与接收内容一致；下载仅导出当前屏幕文本，不读取远端原始文件。
- 同一用户最多保持 3 路环境日志流；切换查看配置不占用新流，双击尚未连接的配置才创建新流，各流的输出、停止和重连互不影响。
- 所有活动连接按 `CONNECTION_IDLE_MINUTES` 验证空闲断开，持续收发数据的连接不应被空闲回收。
- 页面和路由切换后 Web、SSH、日志与数据库交互现场保持；统一连接页的计数、类型、环境和权限正确，关闭操作释放真实资源并在原工作区显示已断开状态。
- 单用户全局连接数达到 `USER_CONNECTION_LIMIT` 后，各客户端和连接类型均拒绝新连接并引导用户先进入当前连接页释放资源。

### 17.4 数据库

- MySQL 和 MariaDB 四类连接模式按配置可用。
- 对象树覆盖全部确认对象。
- SQL 高亮、补全、执行、取消、EXPLAIN、历史和收藏可用。
- 数据库和数据表收藏可以从连接树顶部直达目标，并与 SQL 收藏互不影响。
- 有唯一键的表可以在网格内新增、编辑和删除。
- CSV/XLSX 数据导入和 CSV/XLSX/SQL 数据导出可用。
- 备份文件可以恢复到测试数据库。
- 两个连接之间可以完成一次性结构和数据传输。
- macOS App 的本机数据库集成测试通过 preload API 完成连接测试、查询、取消和 SQL 历史审计；独立运行时测试覆盖 CSV/XLSX/SQL 导出、CSV 导入、备份下载、恢复和跨连接迁移，并验证仅 macOS 私网 `EHOSTUNREACH` 映射为本地网络权限提醒。
- macOS App 的本机连接巡检集成测试通过 preload API 对真实 SSH 测试服务和 Navicat HTTP Tunnel 数据库执行认证与 `SELECT 1`，并验证签名结果落库、审计生成以及返回数据不包含凭据字段。

### 17.5 Redis

- Redis 连接作为独立资源进入个人/组织工作空间、连接组、多环境关系、待分配队列、直接/项目授权、复制、巡检和审计。
- 普通 Web 和桌面本机执行均可连接 Standalone Redis；桌面 Renderer、列表 API、审计和日志中没有密码、TLS 私钥或 SSH 凭据。
- 真实 Redis 7.4 基线覆盖 TCP、逻辑库、UTF-8/二进制键值、String/Hash 写入、SCAN/TYPE/PTTL、INFO、只读连接、未知/高危命令拒绝和无界读取限制。TLS 与 SSH Tunnel 使用与既有数据库/SSH 相同的连接材料和主进程边界，发布环境仍需使用实际证书和目标 SSH 主机验收。
- 键空间遍历只使用 SCAN，返回键去重且不宣称一致性快照；UI 达到 10,000 个保留键后停止追加并要求缩小范围。
- String、Hash、List、Set、Sorted Set 和 Stream 的查看与常用增删改可用；键复制、重命名、UNLINK、相对 TTL 和永久键可用。
- Monaco 命令工作台按同一策略执行读写命令，最多 20 行顺序批量；高危、管理、脚本、阻塞、订阅和未知命令被可信执行端拒绝并审计。
- INFO 和最多 32 条默认 Slow Log 可读取；单响应 2 MiB、集合预览 500 项、扫描 COUNT 10–1000 和命令/参数上限均生效。

### 17.6 服务维护与节点监控

- 一个环境可以录入多个服务，每个服务可以纳管多条 SSH 连接上的多个部署节点；同一 SSH 连接承载多个服务时共享同一主机快照，删除关联不删除远程资源。
- Linux `amd64/arm64` 监控包均包含可执行的主程序、Telegraf 采集子进程、现代/旧版 systemd 单元、安装脚本、安装清单、SHA-256 校验和第三方许可说明；默认安装用户、程序/配置/数据目录权限和重复安装重启行为正确。
- 一键安装预检能区分可安装、Viron 原位升级、目录冲突和无清单旧手工安装，拒绝非 Linux、未知架构、缺少 systemd、无 root/免密 sudo 或服务端包不完整的目标；安装成功后页面直接显示首批指标和托管路径，失败不会覆盖非 Viron 内容。
- systemd 主程序和采集子进程以 UID 0 运行，样本记录 `collectorUser: root` 与当前 `metricsVersion`；同版本但缺少任一能力标识的既有托管节点显示“更新监控服务”，只有用户确认更新后才迁移单元。
- Viron SSH 用户无需 root 密码或 sudo 即可通过仅允许固定 `collect` 动作的 `root:viron-monitor` 本地 Unix Socket 请求并等待一次 root 采集；Socket 不监听网络、不接受命令参数或任意进程执行，pull、clear 和 Kubernetes context 选择继续由 `viron-monitor` 组限制。首次安装或迁移必须具备 root 登录或免密 sudo。
- 一键安装进度弹窗按真实服务端阶段推进，逐文件显示上传进度并提供默认折叠的脱敏日志；关闭弹窗后任务继续且可以恢复查看。失败定位到发生步骤并提供完整重试，重复点击不会为同一 SSH 连接并发启动第二个任务。
- systemd、Docker、Podman 和 Supervisor 扫描结果包含稳定维护标识与归一化状态；Kubernetes kubeconfig/context 可以自动发现并由用户选择扫描范围，Deployment、StatefulSet、DaemonSet 候选按 cluster/context、namespace 和资源类型筛选，包含副本状态与关联 Service；未扫描到的非 Kubernetes 目标可以手工登记，Kubernetes 和普通进程不出现标准启停按钮。
- 宿主机 CPU 分项、内存与 Swap、负载、磁盘容量与 I/O、网络、PSI、温度和运行时间，以及已纳管部署节点能够归属的 CPU、内存、运行时间与重启次数，可以通过现有 SSH/跳板链路进入服务维护页，并按 1 小时、6 小时、24 小时、7 天或 30 天查看时间序列。CPU、内存和磁盘 I/O 展示固定区间 Top 5 加“其他进程”的堆叠组成，悬浮点展示当时真实 Top 5；趋势摘要和自动诊断区间与原始图表使用同一降采样点。进程数据不含命令行、参数和环境变量；节点无需监听端口或访问 Viron。
- 节点断开中心期间样本持续进入 SQLite/WAL 并保留 30 天。重新连通后，中心按“工作空间 + Agent”共享序列游标幂等入库并记录缺口，同一空间的重复 SSH 连接不重复保存或重复计数，不自动删除节点数据；手工清理只影响节点本地缓冲且保持后续序列连续。
- systemd、Docker、Podman 和 Supervisor 的启动、停止、重启只作用于用户确认的部署节点；Kubernetes、停用服务、普通进程、已删除或已移出环境的 SSH 连接被服务端拒绝，并记录成功或失败审计。
- 服务和部署节点均可配置名称、图标和 Shell 正文组成的功能按钮；服务级按钮逐部署节点执行，节点级按钮只执行对应节点。脚本经标准输入发送给 `/bin/sh -s`，正文不进入远端命令行或审计；每个目标的退出码、耗时、stdout/stderr 与截断状态在当次结果弹窗中独立展示。
- 服务日志快捷入口复用环境日志配置并能定位具体日志；管理员负责服务、部署和日志关联配置，有环境及 SSH 连接使用权的成员可以查看并执行节点维护动作。
- 服务维护页按 10 秒只读中心数据无感刷新，入口区分服务数与按 Agent 去重的已监控宿主机数；宿主机工作区默认展示监控，服务发现与监控同级切换。扫描候选按名称、systemd/Docker/Kubernetes、状态和 Kubernetes 维度筛选后以列表展示，进入发现页默认选中第一个有候选的类型；kubeconfig 选择放在 Kubernetes 筛选下方。未选定服务时先选择或录入目标服务再纳管。新 SSH 连接首次自动探测，已识别或托管节点按平台管理员配置的频率持续拉取。
- 中心保留最近 30 天监控样本；历史接口按工作空间与 Agent 去重、约 480 点降采样并保留断档。宿主机页面绘制资源组成、趋势摘要和诊断区间，服务页面为每个部署节点绘制可归属的 CPU、内存、运行时间和重启次数；旧 Agent 缺少新指标时保持兼容并提示更新，不把缺失值伪装成异常占用。
- 环境级告警覆盖宿主机离线、CPU、内存、磁盘使用率、最高温度、磁盘新增/消失/恢复和部署状态；连续两次确认，活动期间去重。宿主机离线同时覆盖 SSH 拉取失败、监控程序缺失、协议异常和采集样本持续未更新，并按工作空间与 Agent 去重。磁盘首次基线静默，新增为一次性事件，消失和恢复分别通知，排除项与虚拟文件系统不参与。所有有环境访问权的用户都有独立未读和提醒状态；站内弹窗点击进入对应环境的服务维护目标，桌面 App 发送可点击的 macOS/Windows 原生通知，普通 Web 在用户授权后发送浏览器系统通知。当前不提供环境组汇总。

### 17.7 安全与交付

- 元数据库、日志、API 和浏览器中没有明文凭据。
- 全新实例自动生成并跨重启复用主密钥；已有密文但主密钥缺失、不可读或不匹配时服务拒绝启动。
- 不同主密钥实例间可以通过密码保护的迁移包导入，全部凭据在目标端重新加密，同一迁移 ID 不可重复导入。
- 审计覆盖确认的所有操作。
- 终端录像与 SQL 历史按保留期清理。
- amd64、arm64 镜像均能启动并通过健康检查。
- Lite 镜像不包含 Chromium 且根路径不提供浏览器客户端；Full 镜像同源提供浏览器客户端并能启动服务端目标网站会话。两者的 SSH/SFTP/日志/数据库/Redis 服务端转发能力保持开启。
- 离线镜像包可以 `docker load` 后运行。
- macOS `arm64` 与 `x64` App 均包含匹配目标架构的运行时，声明最低 macOS 12.0，关键二进制部署目标兼容 Monterey 12.7.6，并可从完整本地静态资源启动；有效 Endpoint 能完成产品/API 协商，非法 Origin、非 Viron 响应和协议无交集被拒绝；只有用户主动打开 Web、SSH、SFTP、日志、数据库或连接巡检能力后才建立对应本机目标连接。
- macOS `.app` 与 `.dmg` 通过自签名深度校验；自签名包不宣称通过 Apple 公证。
- macOS App 包含可执行的 `viron-mcp` STDIO 启动器和 MCP SDK 运行依赖；Broker 描述文件、Unix Socket 和握手 Token 仅允许当前操作系统用户访问。本机 Codex 可以按 App 当前工作空间和执行模式调用资源读取、真实连接和确认操作，App 未运行或未登录时必须明确失败。
- 远程 `/mcp` 使用个人 API Key 完成真实 SDK 初始化、11 个固定网关工具枚举、按领域操作发现、工作空间/环境/文档读取和 Operation 生命周期；平台 Key、匿名请求、跨用户 Session、账号安全路径和秘密导出均被拒绝。
- 版本接口只声明实际存在且文件名版本与服务一致的安装包；匹配当前架构的新版本 App 能通过受限更新窗口完成可取消下载，并由主进程继续完成完整性检查、安装触发、目标版本确认与临时文件清理。当前打包流程验证 macOS 与 Windows 产物包含更新窗口、独立 preload、更新代码和有效安装器；macOS 发布必须在真实系统完成旧进程退出后的跨版本覆盖、签名/版本确认和自动重启，Windows 发布仍必须在对应架构真实系统完成相同跨版本验收。

## 18. 验证入口

长期文档不保存某次运行的测试数量、PID 或临时环境结果。当前提交是否可用应通过以下入口验证：

- `npm run typecheck`：Vue 和服务端 TypeScript 类型检查。
- `npm test`：Fastify API、加密、连接、SSH、Redis、环境日志、导入和备份恢复测试；设置 `REDIS_SERVER_BIN` 时运行真实 Redis 协议集成测试。
- `npx vitest run tests/mcp.test.ts tests/desktop-mcp-broker.test.ts tests/desktop-mcp-security.test.ts`：远程 MCP SDK、个人 API Key、业务目录、凭据安全页面、确认租约、multipart、秘密脱敏、本机 Broker 与安全窗口边界。
- `npm run build`：客户端生产构建和服务端编译。
- `npm run build:desktop`：桌面 App Renderer、主进程和 preload 构建。
- `npm run build:monitor`：交叉构建 Linux `amd64/arm64` 的 `viron-monitor` 与固定 Telegraf 采集子进程，并验证二进制和安装目录完整。
- `cd monitor && go test ./...`：验证节点 SQLite/WAL 序列、30 天保留、只读 pull、兼容 ack、显式 clear、缓冲压缩和缺口行为；采集子进程另以 Linux 目标架构和 `custom` 标签完成交叉编译。
- `npm run package:macos`：当前 Mac 架构的自签名 `.app`、`.dmg` 构建及签名校验；使用 `npm run package:macos:intel` 验证 Intel `x64` 与 Monterey 12.7.6 兼容基线，使用 `npm run package:macos:all` 同时生成两种架构。
- `npm run package:windows:x86|x64|arm64`、`npm run package:windows:all`：Windows 对应架构 NSIS 安装包构建、包内 App PE 架构、NSIS 外壳与运行依赖完整性检查；真实 Windows 运行验收必须另行执行。
- `npm run package:release -- [version]`：在 macOS 构建机上顺序完成源码验证、macOS arm64/x64、Windows x86/x64/arm64，以及分别包含 Lite、Full、Script Runner 的 `linux/amd64`、`linux/arm64` 离线镜像包；省略版本时使用 `package.json` 当前值，传入新版本时永久同步 npm 版本、Compose 标签和版本化文档，并重建发布说明与 `SHA256SUMS`。
- `VIRON_MYSQL_TEST=1 npx vitest run tests/mysql.integration.test.ts --reporter=verbose`：可选真实 MariaDB 集成测试。
- `VIRON_DESKTOP_INSPECTION_TEST=1 npx vitest run tests/desktop-local-inspection.integration.test.ts --reporter=verbose`：源码 Electron 的本机 SSH/数据库连接巡检与签名回传测试；设置 `VIRON_DESKTOP_INSPECTION_EXECUTABLE` 可复用同一用例验证打包 App。
- `REDIS_SERVER_BIN=/path/to/redis-server npx vitest run tests/redis.test.ts`：启动临时真实 Redis，验证连接、逻辑库、UTF-8/二进制键值、扫描、只读和命令策略；不使用或修改正式 Viron/Redis 服务。
- `./scripts/dev-service.sh status` 和 `GET /healthz`：本地开发服务状态与健康检查。

具体测试结果由当前 Git commit、CI 或本次任务输出承担，避免文档中的一次性数字过期。
